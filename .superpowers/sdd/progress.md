Task 1: complete (commits f3d4935b..ddae6dee, review clean)
Task 2: complete (commits 50d6c2d2..75771ccc, review clean)
Task 3: complete (commits df6a4db0..9a959c5b, review clean)
Task 4: complete (commits 9c49b2ba..5e381205, visual and regression verification complete)
Fairy Hollow Original Mock V4 Task 1: complete (commits 0334769f..73fa9a2a, review clean)
Fairy Hollow Original Mock V4 Task 2: complete (commits 0a8fba81..2a404761, review clean after fox and dressing fixes)

## action-button-component (plan 2026-07-21, base 2b7f9870)
Task 1: complete (commit e1c8ae36, controller-reviewed: 8 glyphs correct/transparent/edge-free/family-consistent, intake archived, test-fast 797/797). MINOR follow-up: slice_islands.gd border-only flood-fill resurrects enclosed magenta pockets that chroma_key.gd zeroed — worked around per-asset here, not fixed in tooling.
Task 2: complete (commit 96ba5edf, review clean — Spec OK, quality Approved). Deviations confirmed correct: direct load() for un-baked glyph (skips defringe/feather; OK since Task-1 glyphs pre-cleaned), test print format fixed for run_suites.py regex. MINOR (for final review): builder re-reads config per-build when opts.cp absent — Task-4 home bar must pass opts once (plan already does); redundant tint fallback harmless.
Task 3: complete (commit f2428d21, review clean — Spec OK, quality Approved, 0 issues). Deviations verified correct: DEFAULTS is _default_params() instance method; preview renamed _action_button_row_preview (collision); info_bar preview fallout fixed (home_button_opts_from_config tolerates absent block). Visual /tmp/action_button.png confirmed by controller (8 consistent rugged-edge tiles). MINOR (for final review): stale inert "home_button" block remains in ui_workbench_settings.json (loader is schema-driven, harmless).
Task 4: complete (commit 1ff9febc, review clean — Spec OK, quality Approved). Home bottom bar builds via Kit.action_button; opts built once + duplicated per tile; NAV_SPRITE removed (dead); SpriteButton kept for back/gear/place-picker. Real-path render (map_shot fresh) confirmed by controller: 5 tiles share one rugged edge, Daily-dot + Mail-pill correct. MINOR (for final review): spec.surface field in _bottom_bar_specs now dead data — prune after checking maps-gallery call site.
Task 5: complete (commit 77bb676b, review clean — Spec OK, quality Approved, 0 issues). NOTE: original implementer agent hung twice (backgrounded make test + waited on a notification) leaving uncommitted code with GDScript parse errors (:= on dynamically-loaded GDScript return); controller finished — fixed the parse errors, fixed a latent NAV_BAG-existence guard (would break post-Task-6) by keying construction on KitB==null + gating empty-state re-add on new _bag_well_drawn_disc flag, removed dead NAV_HOME/NAV_BAG consts + SpriteButton import from board.gd. Bag overlay/count/drag preserved. Full sweep 26 suites/1452/0; real-path board render confirmed. Lambda teardown noise pre-existing (baselined 24 vs 19). LESSON: implementer agents must run tests FOREGROUND, not background+wait.
Task 6: complete (commit bbf1eb83, review pending final whole-branch). 8 core baked tiles archived to _archive/nav_baked_tiles_v1/; make test 26 suites/1452/0; controller verified BOTH final renders (home bar + board wells share one rugged edge, cohesive glyphs, badges/count intact). MINOR (final review): board.gd fallback still passes "nav_home.png"/"nav_bag.png" strings to ActionBar.home_well (archived paths) but only reachable if Kit fails to load — degrades gracefully.
FINAL whole-branch review (opus): Ready to merge = YES. All issues Minor. Post-review cleanup commit e3776abd (deleted dead home_bar_tile_opts, dropped unread surface field, removed stale home_button JSON block); full sweep 26/1452/0 after cleanup. DEFERRED (non-blocking): DRY press-feedback duplication between action_button and _apply_deckle_button_surface; eager .get() default eval in action_button when opts.cp present (negligible). Feature COMPLETE.

## ftue-hand-hint (plan 2026-07-23, base ab1d6804, worktree /Users/xup/dh/wt-ftue-hand-hint)
Task 1: complete (commit 23eba3a4, review clean — Spec OK, quality Approved). MINOR (final review): docs/FEATURES.md flag registry not updated for ftue_hand_hint; defensive .get in ftue_seen differs from sibling accessor style (deliberate).
Task 2: complete (commits 4b85b7f3..0ba4e0f8, review clean after fix — Spec OK, quality Approved). Deviations verified correct by reviewer probes: explicit _build() replaces _ready() (size reads 0 right after set_anchors_preset even in live frames), _screen_size() host-size fallback (real fix, not test-only). Fix added real veil-band geometry coverage (38 assertions) + retarget-after-dismiss guard. MINOR (final review): overlap epsilon 0.01 duplicated in two test helpers; _no_self_overlap only exercised in the overlapping case.
Task 3: complete (commits 3d839929..0273259e, review clean after two fixes — Spec OK, quality Approved). Fix 1: flag-off now dismisses a live hint (was un-clearable). Fix 2: new games/grove/tests/grove_ftue_tests.gd drives a REAL in-tree Board.tscn (16 asserts, registered in GROVE_TESTS); revert experiment independently reproduced by reviewer (2/16 fail on buggy ordering, 16/16 on fix); _end_hand_hint flag-off dismisses unconditionally. ACCEPTED TRADEOFF: flag-off does not write the ledger, so a flag off->on round-trip mid-session can re-offer a teach (Features.FLAGS is a code/test-only lever, never a runtime toggle). MINOR (final review): gen_tap-live teardown untested (merge-live only); no docs/FEATURES.md row for ftue_hand_hint; one-frame stale cutouts between _rebuild_all and the awaited present.
Task 4: complete (commits e793af3c..35980ec1). Hand art generated + intaken to ui/kit/hand.png; HAND_OFFSET retuned (18,14)->(29,43) to the real arts fingertip; grove_shot ftue/ftuegen modes added. Controller measured the veil in a real Board.tscn probe (5 bands, 1920x1920 minus 2 cutouts, alpha 0.35 - geometry correct) and found a full-width hairline from fractional band edges; fixed by integer-snapping (commit 35980ec1), re-captured, controller confirmed the seam is gone in both shots. KNOWN/ACCEPTED: the dim covers the BOARD only, not the HUD/quest cards/bottom bar (those live on a separate CanvasLayer a child of the board root cannot cover) - spec said "the whole page dims"; needs a Dev call.
FINAL whole-branch review (opus): NO -> fixed. Critical: hand_hint z_index 4000 sat ABOVE Overlay.MODAL_Z (2048/2110) so the veil punched holes through the first-run How-to-Play card and every modal; fixed to HAND_HINT_Z=500 + a layering_tests guard. Important: every grove_shot mode ran on a fresh save so MODE=played (used by release_screenshots.sh) rendered the hand+dim into App Store shots; fixed by seeding ftue_seen in all modes except ftue/ftuegen. Important: docs/FEATURES.md row added. Plus retarget no longer restarts the loop on unchanged geometry, no double-dim at handoff, dragged piece renders above the veil, fallback fingertip aligned, .uid files committed. Fix commit 5c199fa7; make test 31 suites/1750/0. CORRECTION: my earlier "dim covers the board only" call was WRONG - the reviewer measured a 0.649 brightness ratio across the whole frame; there are no CanvasLayers in this game. DEV CALL (asked, answered): veterans are NOT exempted - everyone sees each teach once.

## content-derived-level-gates (plan 2026-07-25, base 7dbc3654, worktree /Users/xup/dh/wt-content-derived-curve)
Task 1: complete (commit 899e333d, review clean — Spec OK, quality Approved). Derived cluster floors = level_at_coins(cumulative cost); floor ladder [1,2,3,4,5,7,9,11,14,16,19,22,25,29,33,37,41,46,51,56,61,67,73,80,87], scene windows L1-7/8-19/20-37/38-61/62-87. scene_cells_tests 25/25, test-fast 25 suites/1045/0. IMPORTANT deferred to Task 2 (its Step 6 deletes the block): stale grove_data.gd:144-150 paragraph still tells the reader to re-space CLUSTER_LEVEL_STEP by hand. MINOR (for final review): cumulative_cluster_cost doc overstates clamping (negative i returns cluster-0 cost, unreachable); coverup_pages x clusters cost walk duplicated across _cadence_table key, _build_cadence and cumulative_cluster_cost, and the cache key recomputes the walk on every hit.
RECONSTRUCTION NOTE (final review pass): the Task 2/3 lines below were LOST — a `git revert --no-commit e22bbe65` (done to restore this ledger and the task reports after `.superpowers/` was accidentally gitignored) overwrote the on-disk working-tree copy of this file, which had these entries appended but never committed, with the older pre-`e22bbe65` blob (Task 1 only). Rebuilt from commit history/diffs, not the original prose. The Task 4 lines that follow ARE the original text (captured verbatim via grep earlier in this same review session, before the overwrite).
Task 2: complete (commit c2208065, report rewritten at baa27450 after review — reviewed span 899e333d..baa27450). Derived the zone unlock cadence: _build_cadence() spreads each scene's ZONE_BAND zones evenly across its scene_end-derived window, pins zone 0 to L1, forces strict monotonicity; zone_unlock_level/zone_unlock_levels read it, replacing the deleted ZONE_UNLOCK_LEVEL const. Shipped cadence [1,5,8,12,16,20,26,32,38,50,62,75]. mechanics_tests/quest_fence_tests updated to the derived reads.
Task 3: complete (commits c957edc9 — comment-only review fixes dropping stale ZONE_UNLOCK_LEVEL wording + honest strictly-increasing-repair doc — then c31646db, the actual guard). tuning_tests.gd: halving LEVEL_BASE_COINS/LEVEL_STEP_COINS moves the cadence (cache not stale), keeps it strictly increasing and scene-aligned, moves cluster floors correspondingly, and restoring the dials restores the shipped table. tuning_tests 28/28.
Task 4: complete (commit 778167e7, review clean — Spec OK, quality Approved). grove_sim reports per-scene (window · zones · day completed); reporting-only diff (22+/3-), completion hook keys scene_done_day by MAPS z-index before `map` advances, report loop converts position -> page via coverup_pages()[p]; verified no crash on a short run with no completions. make test 33 suites/2015/0. MINOR (final review): "L%d-%-3d" column misalignment in the SCENES table (dev-tool cosmetic).
MEASURED (3 seeds, before 7dbc3654 -> after): clusters at day 60 21/25 -> 17/25; last-content-zone day 32-37 -> 151-155; book-completion day 120-127 -> 258-268. Zero jams, sim PASS, invariant Y PASS on all 6 runs. The whole arc got ~2.1x longer. ROOT CAUSE (controller): the spec's "floors are non-binding at lead 1.0" assumption is WRONG -- floors are derived from cumulative cost measured in CLOCK (quest) coins, but the ladder is paid from the whole wallet, and quest coins are only ~46% of the faucet (seed 1 day 60: clock 12,543 vs spendable 14,590 of 27,133). So the level floor binds hard and roughly doubles the arc. CLUSTER_LEVEL_LEAD is exactly the dial for this (~0.46 = the measured quest share restores non-binding floors). ESCALATED TO OWNER -- not tuned.
FINAL whole-branch review (opus) fix pass: corrected 3 comments asserting CLUSTER_LEVEL_LEAD=1.0 makes cluster floors non-binding (measured false — see the MEASURED line above; floors count CLOCK coins only, ~46% of the faucet, so the floor binds), several comments still describing the deleted ZONE_UNLOCK_LEVEL/CLUSTER_LEVEL_STEP tables (content.gd, grove_data.gd, quests.gd, board.gd, mechanics_tests.gd, quest_fence_tests.gd, grove_explore_tests.gd — values verified against zone_unlock_levels()/cluster_min_level()/gen_retirable() directly), two test assertions (scene_cells_tests.gd, tuning_tests.gd) that hardcoded lead=1.0 instead of reading CLUSTER_LEVEL_LEAD, added 3 missing rows to the plan's level-keyed-reads table (Quests.current_band, SELL_MAP_BAND, gen_quest's tier-bell saturation), and fixed a dangling sentence + a refuted "can no longer drift apart" claim in grove_sim.gd. No dial/formula/MIN_LEVEL changed. make test 33 suites/2015/0. SELF-INFLICTED DATA LOSS (fixed, not fully recovered): `git revert --no-commit e22bbe65` (to undo an errant `.gitignore` dupe + restore 5 deleted-but-still-wanted files) overwrote the ON-DISK working-tree copies of this ledger's Task 2-4 entries and of task-3-report.md/task-4-report.md with their stale pre-e22bbe65 content, destroying newer uncommitted text. Recovered: Task 4's two ledger lines verbatim (grep'd before the overwrite) and task-2-report.md (its last commit WAS the final content, no loss). Reconstructed from commit history/diffs/fresh sim re-measurement, NOT recovered verbatim: the Task 2/3 ledger lines above, and task-3-report.md/task-4-report.md in full (each carries a RECONSTRUCTION NOTE at the top). Lesson for next time: snapshot untracked-but-wanted files before any `git revert`/`git checkout` touching them.
FOLLOW-ON (owner re-spine, 2026-07-26): the cost-derived floors were replaced by an owner-authored SCENE_END_LEVEL, chosen in DAYS via the new games/grove/tools/pacing_calc.gd. Commits: 503f12ea (calculator), 41aa4538 (solve mode), 9ea12685 (SCENE_END_LEVEL model replaces the cost derivation; CLUSTER_LEVEL_LEAD + cumulative_cluster_cost deleted; curve 30/12 -> 1/2; cluster costs x0.092, 46,740 -> 4,290), 818490df (SCENE_END_LEVEL [19,29,39,48,58] -> [25,36,46,58,71], calculator now models LEVEL_WATER_GIFT).
KEY LESSON: the binding resource is WATER, not tapping time (POP_COST 1, WATER_CAP 100 -> 3 sessions = 300 pops/day ~= 5 min of tapping). And LEVEL_WATER_GIFT is a THIRD of the water supply once levels come 2+/day — a pacing model that ignores it under-predicts the calendar by 40% (predicted 25 days, sim measured 16).
ALSO CAUGHT: games/grove/economy_tuning.json is committed and read at boot; it was silently overriding LEVEL_BASE_COINS/LEVEL_STEP_COINS back to 30/12 while every test passed. Any curve change must update it too.
MEASURED (seed 1, verified by controller): scenes done days 2/5/10/17/25, book day 25 (target 25), last content line day 19 = 76% through, 25/25 clusters, 5/5 pages, L71, zero jams, sim PASS. Seed 7: book day 28. OPEN: water self-sustain WARN 37% >= 30%; early-map I2 WARNs (onboarding carve-out); ~12% seed variance in book length. The final whole-branch review predates these two commits and is stale.
Task 7: complete (re-tune, commit pending). The [25,36,46,58,71] solve hit the day-25 total but front-loaded the split (2/5/10/17/25 vs the 3/4/5/6/7-day-per-scene target). Replaced SCENE_END_LEVEL with the levels grove_sim actually reaches at days 3/7/12/18/25 (mean of seeds 1/7/42): [28,41,54,64,71] — measured off the running sim, not re-derived from pacing_calc's model. Updated the derived-table literals in scene_cells_tests.gd (floors, scene windows), mechanics_tests.gd (cadence, arc table), tuning_tests.gd (shipped_cad/shipped_floors guard). grove_board_actions_tests.gd's retire-line test had pinned the retirement boundary as a literal `30` (the old zone_unlock_level(3)) instead of deriving it — replaced with the already-computed `_z3` variable so it tracks the cadence like the rest of the suite. make test 33 suites/2013/0. MEASURED (3 seeds): scene-done days seed1 2/6/12/17/24, seed7 3/7/13/19/24, seed42 2/7/12/18/23 — durations run 2-3/4-5/5-6/5-6/5-7 days per scene, whole book SHORTER than the 25-day target (23-24) rather than longer, because raising the gate levels also raised sell/quest coin income enough to outpace the water-clock slowdown at these particular levels. Zero jams, sim PASS, I1/I2/Y/P1 all PASS on all 3 seeds; I2 early-map ratios and the water-self-sustain WARN (38-41%, unchanged from the prior task) persist as pre-existing parked findings, not introduced by this re-tune. Not re-adjusted further per instructions — reporting the measurement for the owner's next move.
TUNE PASS (commit 1370a44f): SCENE_END_LEVEL [25,36,46,58,71] -> [28,41,54,64,71], taken from grove_sim's MEASURED level-per-day at days 3/7/12/18/25 (mean of seeds 1,7,42) rather than the model. Measured after: cumulative done-days seed1 2/6/12/17/24, seed7 3/7/13/19/24, seed42 2/7/12/18/23 vs target 3/7/12/18/25 — within the +-1 day seed variance except scene 5 (~1.3 short). sim PASS on all three seeds, zero jams, PASS I1/I2/Y. Also fixed a real defect the earlier tasks left: grove_board_actions_tests had re-pinned a retirement boundary to the literal 30 instead of using the computed _z3, so it broke on the new cadence. Further tuning would chase RNG.

## retirement-boundary stale-prose + broken shot modes (2026-07-26)
Comment sync: the three §6 retirement comments (content.gd:397, retire_offer.gd:13, grove_board_actions_tests.gd:245) quoted the pre-re-spine levels (L11/L22/L33 and L12/L38/L75). A peer branch `docs/stale-facts` (b1fa962b) had ALREADY committed the identical fix, unmerged — so instead of writing my own wording I adopted its three code-file blobs byte-exact (e75fdb07) to keep that branch conflict-free, and merged (047debf3). Verified afterwards that main and the peer branch remain byte-identical on all three files, so the adoption did its job. STILL PENDING on docs/stale-facts (rechecked at its e31025b0): only the progression-systems-design §1/§2/§5 prose — the peer dropped its cascade-combos and shelf-design edits once the r3 specs superseded them, so the shelf §15 conflict I first expected no longer exists. That branch also carries no rival grove_shot fix; it is simply behind main and picks mine up on its next merge.
Shot modes (this branch, d3be200f): the FUNCTIONAL half the comment sweep left out. grove_shot.gd "retire"/"retiredone" seeded coins_at_level(14) from when gen_1 retired at L12; MEASURED at HEAD by headless probe: gen_retirable("gen_1",14)=false, retirable_gens=[] — so _maybe_offer_retirement returned early and rendered a plain board, and _retire_line early-returned leaving generator+stock in place. Both captured the WRONG state at exit 0 for months. Fixed by seeding G.coins_at_level(G.zone_unlock_level(3) + 1) in both modes (symbolic, so a re-tune can't re-break it) + comment restated. VERIFIED by controller's own eyes on the merged tree: pre-fix render has no modal and the glow generator + leftover eggs still on the board; post-fix "retire" shows the ALL DONE HERE offer card ("sells 8 leftover pieces for 11 coins", Clear it), "retiredone" shows generator + stock gone with the +11 payout in flight and the coin pill at 11. make test 39 suites/2311/0. No release/App Store tooling consumes these modes (the ios-screenshots skill uses `played`), so the blast radius was the dev gallery only.
OBSERVED, NOT FIXED (pre-existing, present in the pre-fix render too): the HUD's top-left level badge reads "1" in these captures while the NEXT UNLOCK row correctly reads LEVEL 35 — the badge is built from G.level() before the mode seeds coins_earned and _rebuild_all() does not re-tick it. Cosmetic staleness in every level-seeding capture mode, not introduced here. Also untouched per scope: the "fullline" branch (line ~510) still hardcodes coins_at_level(14) under a different tier-gating claim — worth a look since at L14 only line 1 is active.
LESSON: a screenshot/gallery tool has no test coverage, so a data re-spine breaks it silently at exit 0. When a tuning constant moves, grep the tools/ dir for level/threshold seeds too — and seed them from the symbolic accessor, never a literal.

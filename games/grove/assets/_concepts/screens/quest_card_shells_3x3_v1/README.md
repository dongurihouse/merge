# Empty quest card shells — 3×3

Nine border-only quest-card variations on a flat `#FF00FF` key field. Each card interior is deliberately blank; only the outer card construction varies. This is a concept/reference sheet, not a runtime atlas contract.


# Cell improvements — UI mocks v1 (2026-07-26)

Direction mocks for `docs/superpowers/specs/2026-07-26-cell-improvements-design.md` §5.
Each PNG (1080×1920) sits beside its exact generation prompt (`<name>.prompt.txt`).

References used: a live `make shot-grove MODE=hud` capture (layout/style authority) + the
Meadow Sky palette authority (`_concepts/screens/palette_a_meadow_sky_board.png`, new
elements only).

| Mock | Shows |
|---|---|
| `build_mode_1080x1920.png` | build mode: dimmed play, pads on open empty cells, one built Soil, leaf Build button |
| `build_sheet_1080x1920.png` | the two-card build sheet (Soil · Magnet) with counts and prices |
| `soil_growing_1080x1920.png` | a growing piece on Soil (ring, time chip) + the info-bar grow row |
| `magnet_range_1080x1920.png` | a Magnet with its 3×3 field and an auto-merge pull |
| `t7_warning_1080x1920.png` | the t7+ accidental-reset confirm |

Composition mocks, not shippable assets — cell art regenerates through the §8 intake
pipeline when the task is built.

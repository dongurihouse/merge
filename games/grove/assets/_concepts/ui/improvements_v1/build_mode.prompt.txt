Use case: UI mock for a shipped mobile merge game. Asset type: full-frame vertical UI mockup, exactly 1080x1920.
Generate an image (do not write code).

Input images:
- Image 1: the game's REAL current screen — the layout, style, and content authority. Reproduce it faithfully: same autumn cut-paper look, HUD pills, NEXT UNLOCK bar, quest fence, golden board with sealed keyhole cells and egg items, generator stump, and bottom bar (home, cream info tray, bag). Change ONLY what the change list states.
- Image 2: palette & material authority for NEW elements only (matte hand-cut cardstock, warm cut edges, shallow tinted shadows). Do not copy its board or layout.

Style: Meadow Sky + Cut-Paper Playground style: the world is friendly matte hand-cut cardstock — never plastic, clay, sticker, watercolor, gouache, visible brushwork, painterly gradient, glossy 3D, or realistic miniature diorama. Broad, rounded, low-complexity silhouettes; two or three dominant masses per object; each surface one flat base color plus at most one same-hue shadow plane; subtle paper fiber of about 2-4% local value variation; a fine warm cut edge plus a thin locally-darker edge. No universal white sticker outline and no heavy black outline. One upper-left light source. Consolidate leaves, shingles, stones, and ornament into a few readable shapes; never render thousands of tiny details. Features stay legible at phone size. Use the fixed Meadow Sky role palette. No text, labels, numerals, or watermark.
Text exception for this UI mock: existing text visible in Image 1 stays as-is; add NO new text.

Change list (build mode is active):
1. Dim the board play field one step (subtle even darkening of board cells and pieces; HUD, fence, and bottom bar stay at normal brightness).
2. On every OPEN, EMPTY pale-cream board cell (the empty cells of the open center pocket — not cells holding items or the stump, not the brown/mauve sealed keyhole cells), draw a buildable pad: a dashed warm-cream cut-paper outline inset in the cell with a small garden-green plus sign centered.
3. One open cell at the left edge of the open pocket is already built as Soil: a rounded matte earth patch in desaturated gold-brown with a deckled cut-paper edge and one same-hue shadow plane, softly highlighted (not dimmed).
4. A round warm-cream leaf button (about 90 px) with a garden-green leaf glyph, pinned at the board area's bottom-right corner, above the bottom bar, undimmed.

Avoid: glossy plastic, watercolor, brushwork, neon, white sticker outlines, heavy black outlines, any new UI beyond the change list, new text, watermark.

FULL-FRAME MOCK: the whole frame is content — there is no background to key out; save fully opaque.
Save the PNG into: /Users/xup/dh/wt-cell-improvements-mocks/games/grove/assets/_concepts/ui/improvements_v1
Name it: build_mode_1080x1920.png. Report the absolute path.
IMPORTANT: Do NOT ask for approval, confirmation, or direction — the direction is already approved. Do NOT offer a visual companion or read any skills. Generate the image NOW with your built-in image generation and save it to the path above. Then report the saved path.

# Tech & Tooling + Workflow & Process — task log

Engine/code infra, dev tools, tests, perf, save/build · and process: roles, this log, conventions.

_Cleared for a fresh start (2026-06-14). New entries below; see docs/TASKS.md for the format._

### T26 — flag registry index (docs/FEATURES.md) + fix stale features.gd header   ·   2026-06-15   ·   workflow   ·   done
- **Asked:**           §11 wants a flag registry — `FEATURES.md` is absent and flags lack the Lives-in / Eval the spec calls for; `features.gd:2` points at a `FEATURES.md` that doesn't exist; the core non-flaggable set (`gate_pause`, `spot_level_gates`) isn't indexed.
- **Problem:**         Tier-3 spec↔code drift. The §11 registry index never existed as a file, so the header pointer was stale (find-clean) and there was no single place listing every flag's read-site + verdict. No behavior was wrong — only the index/doc was missing.
- **Type:**            follow-up (closes a §11 documentation gap; no flag behavior touched)
- **LLM-reliability:** high for **Lives-in** — each read-site is grep-verifiable (`Features.on("<id>")` → enclosing func), and the FLAGS set was diffed against the doc (24 = 24, exact). **But Eval is owner-subjective** (keep/improve/cut) and was STUBBED, not derived — an LLM cannot make that call.
- **Human-in-loop:**   recommended — the per-flag keep/improve/cut **Eval** verdicts are stubbed `keep — default ON, owner review pending` (truthful: all 24 ship ON, none reviewed for cut) and need an owner sweep. No sibling tasks of this kind yet.
- **Verification:**    every `FLAGS` entry indexed in `docs/FEATURES.md` with a grep'd Lives-in (FLAGS dict vs doc rows diffed → exact 24/24 match, no flag missing/extra); the core list (`gate_pause` → `content.gd:active_giver_count()`; `spot_level_gates` → `content.gd:cell_min_level()` + `board_model.gd:openable_brambles()`) indexed in its own section; `features.gd:2` header now points at `docs/FEATURES.md`; the only code touch is that comment (cannot change behavior — no `make test` run, fresh-worktree reimport avoided per the §11 doc-only scope). Note: `gen_preview`'s read-site is currently PARKED/disabled (T17, `board.gd:1148`) — recorded as such, the flag is held.
- **Iterations:**      1 — grep'd read-sites, derived Lives-in per flag, diffed the set, wrote the registry + fixed the header. Dev miss: N/A (not yet reviewed).
- **Result:**          done — `33938ea`. New `docs/FEATURES.md` (registry: 5 group tables + core section); `engine/scripts/core/features.gd` L2 header repointed (comment only); this entry. Owner Eval sweep parked.

### T35 — batch-merge the 15-branch worktree fleet into main + the giver-bob integration fix   ·   2026-06-15   ·   workflow   ·   done
- **Asked:**          "merge main changes and push" — fold the dispatched parallel-worktree batch into `main` (the dh/merge batch-then-merge convention) and push.
- **Problem:**        15 unmerged `feat/*` branches (T26–T34 + six Tier-2 UX-feel items), each 1 commit, with heavy overlap on `board.gd` (9 branches), the `Makefile` TESTS line, and `merge_spec.md`/`ux-feel.md` — guaranteed conflicts. Plus a T# collision: a `feat/t25-spec-hud-law` branch numbered "T25", colliding with the shipped burst-upgrade T25.
- **Type:**           workflow (integration) — merges other agents' verified branches; not a feature task.
- **LLM-reliability:** high for the mechanical merge (conflicts are localized, git is the oracle, a full test+smoke gate verifies) — **with one caveat proven here:** branch-in-isolation green does NOT imply integration-green. `make test` stayed 661/0 through a real runtime regression that only `make smoke` (stderr) caught (below).
- **Human-in-loop:**  none — the Dev authorized the merge+push; the T# collision was resolved by the project's established renumber convention (no new design call).
- **Verification:**   all 15 merged `--no-ff`, 0 unmerged remaining. Conflicts resolved: the `Makefile` TESTS line unioned 11× (a reusable resolver, `/tmp/fix_makefile_union.py`); `save.gd` (gate-pointer vs spotlight accessor blocks — kept both); `board.gd` featured-quest reward (combined T29's gem premium-bonus `Save.add_diamonds` with T32's icon+number FX API — `floating_reward("gem", …)`); `ux-feel.md` (T27 + the renumbered T34, union). **Gate: `make test` 661/0 across 17 suites (16 engine + grove) + `make smoke` clean, exit 0.** Final tree: 30 commits ahead of origin (15 feature + 15 merge), pushed.
- **Iterations:**     2 — (1) the first full gate was test-green (661/0) but `make smoke` surfaced a runtime error spam: `_giver_bob_stop` read a `bob_tw` meta that was never set (givers that never bobbed) — Godot 4.6 logs the missing-key error even on `get_meta(key, default)`. A seed-at-init pass didn't fully cover it; (2) bulletproof `has_meta` guards on every `bob_tw` read (board.gd `_giver_bob`/`_giver_bob_stop`) → smoke clean, re-gated 661/0. From `feat/t2-giver-bob-payable`; behavior-identical, kills the log spam.
- **Result:**          done — 15 branches integrated to `main` + the giver-bob fix; `make test` 661/0, `make smoke` clean; pushed to `origin/main`. T25 collision resolved by renumbering the spec-hud-law entry → **T34**. **Reliability note for the next batch merge: gate every batch with `make smoke`, not just `make test` — an integration error invisible to assertions hid behind a green suite.** Untracked strays left alone (`games/tools/montage.gd`, `docs/design/grove_art_pipeline.md` — not from these branches).

### T38 — Map-model tails: `zone`→`map` symbol sweep + orphaned `furn_fh_*` cleanup   ·   2026-06-15   ·   tech-workflow   ·   done (a+c); (b) parked art-lane
- **Asked:**           Map-model remaining follow-ups (T21 parked tail): (a) the `zone`→`map` symbol rename across the §6/§7 code + tests + sim (T21 Decision #1, deferred); (b) real §16 map images + on-image spot placement (art lane, owner re-places via the Layout editor); (c) orphaned old-id `furn_fh_*.png` sprites now unused — asset cleanup.
- **Problem:**         T19/T21 left the codebase running a MIXED `zone`/`map` vocabulary mid-migration, and the two halves are NOT synonyms — `zone_done(z,u)` (per-map spots) coexists with `map_done(u,gates)` (whole-atlas), and `map_complete(z,u,gates)` (spots+gate) is a third predicate. The mix is an active foot-gun (a reader can't tell per-map from whole-atlas by name). The rename's real job is to UNIFY onto `map` AND resolve those collisions, not a blind sed. T21's "now-collision-free" referred to the merge-lane, not symbols — there ARE symbol collisions.
- **Type:**            follow-up (of T21, `6f9faf9` — the deferred Decision #1).
- **LLM-reliability:** **(a) high** — deterministic, and the full suite (16 engine + grove) + the Monte-Carlo sim are the behavior oracle; a missed/wrong rename fails a test or a sim invariant. **(c) high** — grep proves the ids unused, then delete. **(b) low** — perceptual/art + owner-only placement (the Layout editor is the owner's tool); not autonomously completable.
- **Human-in-loop:**   **(a)/(c) none** (suite + grep verify). **(b) required** — real map art is an LLM-image-pipeline + owner-aesthetic task and the spot placement is explicitly the owner's (Layout editor); surfaced + parked, not attempted. Sibling low-reliability art tasks: Grove v1 art (T36), the §16 pipeline.
- **Decision (target vocabulary — recorded here; merge_spec uses "zone" 0×, so no spec prose change):** rename every `zone`-as-"a map/level" symbol to `map`; do NOT rename already-correct `map_*` symbols (stay in scope); migrate the one save key + one data field. **Four collision-resolved exceptions (NOT mechanical):** (1) `zone_done(z,u)` → `map_spots_done(z,u)` — NOT `map_done` (taken by the whole-atlas predicate in quests.gd); (2) `zone_complete(z)` (map.gd scene wrapper of #1) → `map_spots_done(z)`; (3) `Quests.zone()` (a func literally named `zone`, returns the frontier) → `Quests.current_map()` — `map` would shadow the generic concept; (4) map.gd scene-local `var zone: Dictionary` → `var map_data` (avoid aliasing the Map scene). Data: `GENERATORS[].zone`→`.map`; `ZONES`→`MAPS`, `ZONE_DIAMONDS`→`MAP_DIAMONDS`; save key `last_zone`→`last_map` (migrated); layout `_data` key `"zones"`→`"maps"` (back-compat read; placements.json carries no `zones` data, so no file migration). Historical comments naming removed symbols (`ZONE_RAMP`, "ZONE-SCOPED") left verbatim — they document the past by its real names. **+quests_zone→quests_map found during the sweep** (a second persisted grove key; migrated alongside last_zone).
- **Verification:**     **(a)** zone(ci) 440→0 live identifiers (only my migration/back-compat code + one `ZONE_RAMP` history comment name "zone", all intentional). **All 17 suites green with pass counts IDENTICAL to baseline** (save 35/35 — +3 new migration tests; the other 16 unchanged: mechanics 50, quest 32, gate_unveil 29, anchor 27, grove 282, …), proving behavior-neutral. `make smoke` OK (the T35 lesson — assertions miss runtime errors). **grove_sim PASS across 6 seeds** (42/123/7/99/256/2025): 0 jams, no-strand holds, I2 steady-state maps-3+ under 30%; seed 123 reaches full 40/40 spots + 5 gates (matches the backlog's validated state). Diff scope checked: every one of the 23 changed code/test files genuinely contained `zone` (save_tests is the lone add — the new test). **(c)** each old fh id referenced only by the save-migration map + orphaned placements.json (string ids, never the file) + no `.tscn`/`.tres` ref; visually confirmed `furn_fh_chest.png` is a chest (old `fh_chest`, now `fh_hearth` — art no longer fits); removed 8 PNG + 8 `.import`; post-delete smoke OK, ghost_preview 16/0, grove 282/0, no load errors.
- **Iterations:**       2 — (1) first rename pass mostly no-op'd: zsh does NOT word-split an unquoted `$FILES` scalar, so `perl … $FILES` got one newline-joined "filename too long"; only the `$(find …)`-based bare-pass partially applied → reverted (`git checkout`, 440 restored) and redid with a zsh array `${(f)"$(…)"}` → clean 440→5-comments. (2) added a save_tests case for the new key migration (saves are where silent data loss hides) — 32→35. No Dev miss (self-caught).
- **Result:**          **(a)+(c) VERIFIED done** — `c9edafb` (rename + save migration + test) · `3c50b04` (sprite cleanup). Behavior-identical, full suite + smoke + 6-seed sim green. **(b) PARKED (art-lane · owner)** — NOT attempted: real §16 map images are an LLM-image-pipeline + owner-aesthetic task and on-image placement is owner-only (Layout editor); confirmed **no engine gap** (the map view auto-wires `map/map_<id>.png`, the editor is live). Backlog item reduced to (b). **Surfaced to Dev:** (i) the unlogged **Wave-1 quest-fence extraction** (`1edbdea`, `core/quests.gd`) has no task entry — ledger gap, and "Wave 1" implies more refactor waves may be in flight; (ii) `data/placements.json` still carries 7 orphaned old-id fh entries (dead; (b)'s re-placement overwrites — left as positional reference, noted in the backlog).

# Mechanics & Systems — task log

Rules & loops: merging, generators, quests, progression, economy logic, gates, win/lose.

_Cleared for a fresh start (2026-06-14). New entries below; see docs/TASKS.md for the format._

### T17 — Per-zone generators + merge-to-evolve (engine mechanic)   ·   2026-06-14   ·   mechanics   ·   done
- **Asked:**          "split [the per-zone-generators epic], use placeholder for art, focus on engine."
- **Problem:**        generators today **accumulate per-chapter and are never consumed** (`active_gen_indices(chapter)` content.gd:61; `set_active_gens` only adds, board_model.gd:53), **can't be moved** (board.gd:1513), and there is **no evolve-merge / line-retirement**. Spec §6 wants a **per-zone roster** (≤4 live, **2 lines each**) where a granted generator **merges onto an old one** (old consumed, old lines retire).
- **Type:**           new — foundation for the parked grove-map + art tasks; live gate-quest wiring depends on §7 (quests).
- **LLM-reliability:** high — pure, deterministic board/roster logic, fully self-verifiable headless. (Art is the low-reliability piece — parked.)
- **Human-in-loop:**  none for the mechanic; recommended later for the congestion-sim feel call (z4–5 = 8 live lines) and the grove content map.

  **Scope (in):** new per-zone generator data SHAPE (`zone` + 2 `lines` + `evolves_from`); roster-derivation logic (live set / live lines / retired lines / evolve lineage); the evolve-merge board op; line-retirement bookkeeping; generators as movable/swappable pieces (never sold, never normal-merge); save-schema slice (live set + retired lines) with a v3 migration; headless tests; sim + grove placeholder-roster kept runnable (placeholder/code-drawn art).
  **Scope (parked):** the real grove 16-gen/32-line themed map + lineage + names (content task); ~256 item + 16 gen sprites (art task); the authored t8 gate-quest + generator-grant quests (lands with §7); the Collection archive of retired lines (its own backlog item — this task only *flags* a line as retired).
  **Decisions (recorded here; player-facing ones → spec §6 note):** (1) roster is a flat data array, engine derives per-zone sets — keeps the 16/32 roster as data. (2) Retirement = stop generating + stop asking that line; **items already on the board stay** (mergeable/sellable), no sweep, no loss; Collection hook is parked. (3) **Interim, until §7:** generators are granted on **zone entry** and the evolve-merge op is exposed for §7 to drive via the grant quests — so the live game runs on the new model without the quest rework. (4) `gen` is a movable piece but **refuses sell and normal-merge**; the only gen-merge is grant→target evolve.
  **Acceptance (observable, headless):** roster fns return the right live/retired lines + lineage; evolve consumes the old gen, installs the new at its cell, retires the old's lines; a retired line is never popped/asked; a gen moves + swaps but can't be sold or normal-merged; save round-trips the new fields and migrates a v2 blob; `make test` green; `grove_sim` runs on the new model with 0 jams.
- **Verification:**   **DONE for the engine mechanic** (economy balance split out → backlog; quests = §7, next). Built + verified: per-zone roster + evolve op + retirement + cell/live-set resolver (pure); a **stateful, persisted `gens` map** in `board_model` (cell→id) — **movable generators (#1)** via `move_gen`, the **player-driven evolve ritual (#2)** via `evolve_gen` (drag a grant onto its predecessor → old consumed, new installed, lines retire; never sold / never normal-merged), **save round-trip (#3)** through `to_dict`/`from_dict` + a seed-if-empty load migration; **dead preview helpers removed (#5)**. Evidence: `mechanics_tests` 34/34 (roster + evolve + move + persist), `grove_tests` 304/304 (incl. **12c**: a live board seeds zone 0, moves a generator + re-renders, evolves it in place + re-renders), `layout` 21, `save` 26 — `make test` **385/385**; `make smoke` OK. Board input rewired cell-keyed (`_release_gen` routes tap→pop / drag→move / grant→evolve).
- **Iterations:**     1 build pass after the model TDD; no Dev-caught misses. (The earlier 4 sim tuning passes informed the economy split-out, not this.)
- **Result:**         **DONE (engine mechanic), shipped 2026-06-15.** Generators are per-zone, stateful, persisted, movable, and player-evolvable. **Intentionally left "broken" past the seeded zone (owner: "let it break"):** there is **no in-play grant DELIVERY** — that is §7's authored grant quests (the next task), so a fresh game sits on its seeded zone's generators until §7 wires delivery. **#4 economy rebalance** (sim still I2-red at uniform `t2–4`, no jam / no strand) **split to its own backlog item** (folds into §7's Monte-Carlo pass). `grove_sim` runs (exit 1, economy-red) by design. Recommend **§7 next** (delivers grants + balances the economy). Committed `a7cfa11`, `--no-ff` merged to `main` as `df23351`, pushed to `origin/main`; the four parked pieces (grove map + art + §7 wiring + #4 economy) are all tracked in `docs/BACKLOG.md`.

### T18 — Retire the evolve-merge; scaffold the generator-grant hand-in (engine)   ·   2026-06-15   ·   mechanics   ·   done
- **Asked:**          "review the backlog item, it may be somewhat outdated … fix the generator code [and] scaffold the grant hand-in."
- **Problem:**        the code still ran T17's **drag-merge evolve** (a grant generator dragged onto its predecessor), but spec §6 **retired that** — generators now arrive via an **authored generator-grant hand-in quest** (deliver an old generator → receive a new line; no on-board merge). Stale across the tree: `gen_can_evolve`/`gen_evolve`/`evolve_map` (content.gd) + `board_model.evolve_gen` + the `board.gd` drag-to-evolve branch + the `evolves_from` data field + "merge-to-evolve" comments.
- **Type:**           follow-up (of T17 / `df23351`).
- **LLM-reliability:** high — pure, deterministic roster/board logic + a delivery branch, fully self-verifiable headless.
- **Human-in-loop:**  none for the mechanism; the live-quest *scheduling* (which boundary fires which grant) is §7's call (its sim).

  **Scope (in):** rename `evolves_from`→`grant_from` (data + engine + tests); replace the evolve op with the hand-in — `gen_can_grant`/`gen_grant` (predecessor found **by id** anywhere on the board, so a *moved* generator is handled), `board_model.grant_gen`; the authored-quest helper `content.gd:grant_quests_for_zone` + a `board.gd` grant-delivery branch (`_deliver_grant`, fires `grant_gen` on a `{grant}` quest); remove the drag-to-evolve branch (generators are movable-only); scrub the "evolve" vocabulary; fix the `grove_sim` comment.
  **Scope (parked / §7):** **scheduling** the authored grant quests into the live `chapters()` (would perturb §7's in-progress economy sim + the 304-test suite) — left to §7 with the Monte-Carlo pass; the t8 gate quest; the Collection archive of retired lines.
  **Decisions:** (1) the hand-in finds the predecessor **by id** in the live set (not a passed cell) — so a player-moved generator hands in correctly, installing the grant at its *current* cell. (2) **Interim unchanged:** the per-zone auto-seed (`live_gen_state` on zone entry) stays the resolver; `grant_gen` + `grant_quests_for_zone` are the scaffold §7 drives — the same "mechanism built, §7 wires delivery" posture T17 left. (3) Kept `move_gen` (movable, §6); a generator dropped on another now just snaps back.
  **Acceptance (headless):** grant fns return the right lineage / live-set; `gen_grant` consumes the handed-in generator, installs the new at its current cell, retires the old lines, and is a no-op when the predecessor is absent; the board re-renders the grant; all suites green; the sim runs on the renamed API.
- **Verification:**   **DONE.** TDD: rewrote the `mechanics_tests` generator section to the grant API first (RED — parse-fail on missing `gen_can_grant`/`gen_grant`/`grant_map`/`grant_quests_for_zone`), then implemented to green. Evidence — `mechanics_tests` **40/40** (roster + grant + the moved-predecessor edge case + grant-quest authoring + move + persist), `grove_tests` **304/304** (incl. **12c**: a live board seeds zone 0, moves a generator + re-renders, then **grants z1a in place via hand-in** + re-renders), `layering` 29, `layout` 21, `save` 26, `smoke` OK; `grove_sim` runs to its verdict on the renamed API (exit 1 — pre-existing I2 economy-red, by design → §7 / #4).
- **Iterations:**     1 build pass after the test rewrite; no Dev-caught misses.
- **Result:**         **DONE, 2026-06-15.** Evolve-merge retired; the generator-grant hand-in is scaffolded (model + board path + authored-quest helper) and exercised by tests, ready for §7 to schedule into the live quest script. **Not committed** — awaiting Dev review (working tree dirty on `main`). Backlog updated: the per-zone-generators item + the §7 item reflect the new code state.

### T19 — Generated quests + calculated reward (§7, the rest)   ·   2026-06-15   ·   mechanics   ·   done   ·   *(followed T18)*
- **Asked:**          "review the backlog item, it may be somewhat outdated, let's fix the generator code, give me the steps to fix those [Generated quests + calculated reward §7]."
- **Problem:**        With T18's grant *mechanism* now scaffolded, the live quest model is still the **OLD fixed script**, not §7's generated model. `chapters()` (content.gd:237) precomputes a per-chapter (= spots-bought) pool from `ZONE_RAMP`; stars are **ask-count** (`_quest_stars` content.gd:290 — 1/2/3 asks → 1/2/3★); **quests pay no coins** (`earn_stars` only, board.gd:1826 — the backlog's `board.gd:1902 add_stars` anchor is stale, renamed by progression rework `2940b0f`); the fence serves the **full pool with NO metering** (gate_pause removed, board.gd:537-541); and there is **no STAR_CAP, no expected-clicks reward, no featured quests, no gate quest, and the authored grant quests are never scheduled into the live stream**. Spec §7 wants generated, level-scaled, metered-to-next-unlock quests with a click→value→stars+coins reward.
- **Type:**           follow-up (of T17 `df23351` + T18; delivers the §7 quest model T17/T18 left scaffolded).
- **LLM-reliability:** **mixed.** Engine (ask-generator, reward formula, metering, save migration, grant/gate scheduling) = **high** — deterministic, headless-verifiable. **Balance/feel** (STAR_CAP, click→value rate, level→asks/tier curve, LEVEL_STARS recal, featured rate) = **low** — needs the Monte-Carlo sim as the check AND an owner eyeball on pacing; the kind that ships wrong on a vibe.
- **Human-in-loop:** **recommended** — three Dev calls surfaced before build (scope/sequencing; gate-quest flow change now-vs-defer; quests-pay-coins interim before the hub sink exists). Feel-tuning recurs with the #4 economy item + the burst-pop item (same low-reliability balance family).
- **Reconcile at pickup (2026-06-15):** the evolve→grant retirement was found green + recorded as **T18** (uncommitted, awaiting Dev review); BACKLOG's §7 item already updated to show the grant *mechanism* done. So T19 = the **remaining** §7 work only. Confirmed `make test` **420/420** (mechanics 40 · save 26 · layout 21 · layering 29 · grove 304). Two record-only ledger gaps still open (progression `2940b0f`, T16 data/skin split — work merged, just unentered).
- **Scope (proposed, pending Dev sequencing call):** P1 the live ask-generator + click→value→stars+coins reward + STAR_CAP + featured · P2 the metered fence (gate_pause) + live-quest save model + v3 migration + affordability floor · P3 schedule the authored boundary quests into the live stream (T18's grant-quests; the gate quest = deliver t8 → unlock next map) · P4 the Monte-Carlo no-strand sim (the verification spine; folds in the #4 economy rebalance) · P5 grove tuning + balance to sim-green. Parked-out: burst-pop (§4/§6), hub-yield loop (KEYSTONE), Collection archive, real grove roster/art.
- **Verification:**   **DONE.** Built P1–P5 in order (Dev calls answered: sequencing=in-order · gate=build-now · coins=on-now). **Engine-only**, grove supplies only tunables in `grove_data.gd`: `content.gd` (`gen_quest`/`quest_reward`/`active_giver_count`/`gate_quest`/`zone_cheapest_spot` + gate-gated `zone_unlocked`/`frontier_zone`/`map_complete`), `board.gd` (the live fence — `_refill_quests`/`_meter_target`/`_deliver_gate`/`_deliver_grant`/`_pending_grant_quests`, save `quests`/`quests_zone`/`gates` + a pre-§7 migration), `board_model.gd` (`place_surplus_gen`), `map.gd` (gate-aware unlock; per-spot gift removed). Evidence — **new `quest_tests` 32/32** (capped ★ + coin overflow · level-scaled/newest-weighted/never-t8 ask-generator · metering · climbing gate ceiling) · `grove_tests` 291/291 (live delivery pays ★+coins & leaves the metered fence · the gate-gated unlock · the full gate→hand-in flow: deliver top-tier → next map unlocks → surplus appears → hand the satchel in → z1a live) · `make test` **439/439**, `make smoke` OK. The **Monte-Carlo sim** (`grove_sim`, rewritten for the generated model) **PASSes across seeds** (42/7/123/999): I1 no-jam · no-strand · I2 steady-state water-gift <30% (map-1 FTUE a documented WARN) · Y selling-not-income; `QUEST_LEVELS_PER_TIER` tuned 4→2 for robust steady-state I2.
- **Iterations:**     several sim-driven passes — found + fixed two bot bugs (retired-line clutter never cleared; the `_pop` generator-selection `break` only broke the inner loop, so one wanted line was never popped → 2-ask quests stalled) + a parse-collision and two `scn.quests` type-infers in the test rewrite. No Dev-caught misses.
- **Result:**         **DONE, 2026-06-15.** The live game runs the full §7 model — generated metered quests paying capped ★ + coin overflow, the great-spirit **gate quest** gating each map's unlock, and the **generator hand-in** opening each new map; `chapters()`/`ZONE_RAMP`/`_quest_stars`/`_stretch`/`lines_debuted` + the per-spot water gift are retired. **PROVISIONAL tunables** (`STAR_CAP=3`, `CLICK_TO_VALUE=1.0`, `QUEST_LEVELS_PER_TIER=2`, `GATE_TIER_BASE=5`/`GATE_ASK_COUNT=3`, featured `0.15`…) are sim-green but **await the owner's pacing sign-off** (the low-reliability feel call). **#4 economy folds in** — steady-state I2 was carried by the §7 tier knob, so the §3 `LEVEL_STARS` curve was left untouched (a recalibration is still available if the owner wants faster/slower leveling). **Not committed** (tree dirty on `main`). Parked-out: burst-pop (§4/§6), hub-yield loop (KEYSTONE), Collection archive, real grove roster/art.

### T21 — Map model: one image = one map (drop overworld + interior; map-select + home hub)   ·   2026-06-15   ·   mechanics   ·   open
- **Asked:**          "review the backlog item … fix the [world] generator code … yes to all 3 [rename now · hub nav+designation only · waysides removed now]; use subagents to parallelize; the other live changes / parallel threads are finished."
- **Problem:**        the world code is the OLD model — a free-pan overworld (`vista`, `MAP_SIZE` 2160×2880) of POI buildings tapping into walk-inside `interior` rooms, plus on-map wayside coin-sink cosmetics. Core §8 (rewritten) + `grove_spec §3`: a **map IS one self-contained image** (open space + buildings/props), **no overworld, no interior**; discrete maps via a **map-select**; the **Farmhouse is the permanent home hub** (deeper surface + a HUD home shortcut); the coin sink moves to the hub yield loop (separate KEYSTONE item).
- **Type:**           follow-up (the world half of the §8 rework; rides the `zone`→`map` rename. Generator/quest §6/§7 = T17–T20, committed: `ea58c35`/`aa249e6`).
- **LLM-reliability:** **mixed.** Scene restructure + data reshape + nav + save + rename = **high** (deterministic, headless: smoke + grove_tests + the suite). Map-image LAYOUT + the select's look = **low** (perceptual) — but real map art is PARKED (§16), so v1 runs on **fallbacks** + the owner re-places spots via the Layout editor once art lands; the visual sign-off defers to the art lane.
- **Human-in-loop:**  recommended for the eventual map-image spot placement + select polish (low-reliability visual, deferred with art); none for the model/nav/save logic.
- **Reconcile at pickup (2026-06-15):** the plan's anchors were pre-`ea58c35`/`aa249e6`; re-grounded on the committed tree. `chapters()`/`ZONE_RAMP`/`lines_debuted`/`_quest_stars`/`chapter_gift` are gone (T19); the map API is gate-aware (`map_complete`/`zone_unlocked(…,gates)`/`frontier_zone(…,gates)`/`zone_cheapest_spot`); `aa249e6` already used **T20** (grove 12-gen/24-line roster), so this is **T21**. `make test` GREEN at baseline (grove 291/0; the loop reached the last suite ⇒ all prior green).
- **Decisions (Dev-approved):** (1) the blanket `zone`→`map` symbol rename is **DEFERRED** — taking call #1's own "scope it / defer" rec: the behavioral rework is the value and is green; a 400-line rename over the just-shipped §6/§7 generator/quest/sim code is cosmetic churn whose only un-tested risks (save-key orphan, placements.json key, the `GENERATORS[].zone` field) aren't worth running over fresh code mid-task. The codebase already runs mixed map/zone vocab (T19 added `map_complete` beside `zone_done`); deferring just holds the status quo. Parked as a now-safe (lane committed → no collision), suite-verifiable mechanical follow-up — surfaced to the Dev to run now if they'd rather. (2) hub = navigation + designation + a `kind` seam only; the upgrade→yield LOOP stays the [P0] keystone item. (3) waysides removed now (the only live coin sink → a temporary gap the keystone fills next).
- **Scope (in):** grove_data `ZONES` reshape (drop `map_pos`/`MAP_SIZE`/`POI_SIZE`; farmhouse → §3 hub roster + `hub` flag + `kind` seam; bg art convention) · content.gd wayside removal · map.gd rewrite (single-image renderer + map-select; remove interior/pan/overworld/waysides; keep buy/level-gate/variant/place-spots/HUD/settings) · the HUD home shortcut + board Home/Decorate rewire · grove_sim + features + dev-tools cleanup · tests rewritten · the `zone`→`map` rename.
- **Scope (parked):** the hub upgrade→yield loop (KEYSTONE, separate) · real map images + spot sprites (§16 art) · Collection archive.
- **Verification:**   **DONE — verified.** `make test` **404/404** (save 26 · layout 17 · mechanics 40 · layering 29 · quest 32 · grove 260; the layering guard confirms scenes→ui→core intact after the map.gd rewrite + the HUD home button). `make smoke` OK (Map.tscn + Board.tscn instantiate). **Visual** — 3 quiet captures looked at (not eyeballed-blind — composited views): (1) fresh map = the 8 hub spots placed on the fallback image, title "✿ 31★ left", Hearth/Kitchen affordable + breathing, the rest level-gated "Lv N", the HUD home (◁) + the bottom atlas/shop/gear + "Tend the garden ▶"; (2) map-select = 5 cards, Farmhouse unlocked/highlighted, Barn–Meadow "✿ after <prev>" locked; (3) progress = 3 owned spots as variant-tinted chips + the open customize strip (base✓/25🪙/2💎) + level-scaled pricing. The single-input-surface rule (`_all_ignore(content)`) holds in both views (test-asserted). Built via **4 parallel subagents** (map.gd rewrite · tests · HUD+board wiring · sim/features/tools cleanup) over the data/content/layout core I cut first. **Spot placement on the real map image is PARKED** with the §16 art — v1 runs on the fallback panel; the owner re-places via the Layout editor when images land (the low-reliability visual call, deferred).
- **Iterations:**     1 integration pass. One cross-cutting bug surfaced + fixed: `save.gd`'s `_SPOT_ID_RENAMES` had the farmhouse rename **inverted** (it rewrote the new `fh_hearth` back to the dead `fh_chest` on every read) — caught by the tests-agent (grove crashed 62/5 → it isolated the one-liner), fixed in Phase C (prior-live→new direction, idempotent). No Dev-caught miss (pre-review).
- **Result:**         **VERIFIED done (behavioral rework).** Single-image maps + map-select + home-hub shortcut shipped; interior/overworld/waysides removed; farmhouse recast to the §3 hub roster (+`kind` seam, yield loop deferred to KEYSTONE); save migration fixed; dev tools repointed (`map_shot`/`click_spot`) + retired (`click_back`/`click_wayside`); grove_spec §3/§5 caught up to the model. **NOT yet committed** — holding for the Dev's call on the deferred `zone`→`map` rename (commit the rework as-is + rename as a follow-up, or fold the full rename into this commit first). Parked (unchanged): hub upgrade→yield loop (KEYSTONE), real §16 map images + spot sprites, Collection. **— UPDATE 2026-06-15:** the Dev's call was made — committed as-is as `6f9faf9` with the `zone`→`map` rename deferred (Decision #1), then integrated to `main` alongside T22/T23/T24 in this batch.

### T22 — Home-hub yield + upgrade-levels loop, Part A (the KEYSTONE coin sink)   ·   2026-06-15   ·   economy   ·   open
<!-- NB: T21 was taken in parallel by the map-model rework; this keystone task is T22. -->
- **Type note:**      renumbered T21→T22 at 2026-06-15 (collision with the parallel map-model T21; that task is the entrenched T21, referenced across BACKLOG).
- **Asked:**          (keystone P0 pasted) "review the backlog item, it may be somewhat outdated, let's fix the generator code, give me the steps to fix those … just focus on what needs to be revamped" → then **"start working on part a in a new worktree."**
- **Problem:**        hub spots are **binary owned/unowned** (no level, no yield); coins sink **only** into net-zero cosmetics — the "coins have no power" tension §8/§10 resolve. **Part A:** a restored hub spot carries a **coin-fed upgrade level** (L1→Lⁿ — richer look + higher yield), and yield buildings **accrue coins** to a per-building daily cap, swept in **one collect-on-return** beat.
- **Type:**           new (delivers the §8/§10 KEYSTONE soft-currency sink; pairs with the §7 faucet (T19) + the shop sinks).
- **LLM-reliability:** **split.** A1 (save schema) = **high** — deterministic, headless-verifiable. A2/A3 (rates/formulas) high once specced. A5–A7 (upgrade/collect UI + leveled look) med/low (perceptual). A9 (the "**extends, never self-sustains**" cap) = **low** — economy-feel, needs the Monte-Carlo sim + owner pacing sign-off (same low-reliability family as T19's tunables · #4 · burst-pop).
- **Human-in-loop:**  **recommended** — yield/upgrade RATES, the per-building cap (≈ a day), and the self-sustain invariant are an owner pacing call (via the sim), like the §7 tunables. **none** for A1.
- **Reconcile at pickup (2026-06-15):** keystone is parked **P0** in `BACKLOG.md`, not yet a `T#`. Audited the **committed HEAD** (`bc648cc`), **not** the dirty `main` tree (the **map-model rework** is uncommitted there). Found the item's anchors stale (`map.gd`→`scenes/map.gd`, lines drifted), the **waysides** coin-sink being **deleted** by that rework, and two **unstated deps**: the `kind`/`hub` seam exists **only in the uncommitted rework**, and the generator **burst-upgrade (Part B)** needs **burst-popping** first (separate item, `feat/burst-pop` worktree). Reported; Dev chose **Part A in a worktree**.
- **Scope (this session):** **A1 only** — the save schema, the one slice independent of the rework. **A2–A9 queued** (they edit `content.gd`/`grove_data.gd`/`map.gd`/`hud.gd` + need the rework's seam; building on HEAD would **duplicate** it → conflict).
- **Verification:**   **A1 DONE.** TDD RED→GREEN. **RED:** `save_tests` parse-failed naming the 4 missing accessors (`spot_level`/`set_spot_level`/`hub_collected_at`/`set_hub_collected_at`). **GREEN:** `save_tests` **32/0** incl. 6 new (spot level default 0 · store+persist across reload · unrelated spot stays 0 · `hub_collected_at` default 0.0 + persist · `levels` carried by the spot-id rename migration). Full `make test` **445/0** (save 32 · layout 21 · mechanics 40 · layering 29 · quest 32 · grove 291); no regression — only additive `save.gd` accessors + one migration-loop key (`levels`).
- **Iterations:**     1 (clean RED→GREEN; no Dev-caught miss).
- **Result:**         **OPEN.** A1 (save schema — `spot_level`/`set_spot_level` + `hub_collected_at`/`set_hub_collected_at` + `levels` rename-migration in `save.gd`, + `save_tests` §15–16) committed on **`feat/hub-yield`**. **NOT merged** (Part A incomplete), **NOT pushed**. A2–A9 **blocked on the map-model rework merging** — pick up against the `kind`/`hub` seam then. Worktree `.claude/worktrees/hub-yield` retained. **— UPDATE 2026-06-15:** A1 merged to `main` via `a4168d4` (branch `bd234ba`); the `levels`-rename test realigned to T21's corrected `fh_chest`→`fh_hearth` direction. A2–A9 now unblocked (map-model is in `main`).

### T23 — Burst-pop + the burst-upgrade coin sink (§4/§6)   ·   2026-06-15   ·   mechanics   ·   done   ·   *(followed T20; renumbered T21→T23 at merge — parallel-thread collision, map-model is the entrenched T21, hub-yield→T22)*
- **Asked:**          "evaluate this [Burst popping + energy-faucet backlog item] … do you need me to make any decisions" → owner chose **1a** (defer the two energy-faucet changes — +50 gift, 1/day refill — to the parked §7 pacing pass, NOT this task) + **2b** (build the **burst-upgrade coin sink** here); "start in a new worktree."
- **Problem:**        the code popped exactly **ONE item per tap** (`_pop_seed`); no `BURST_ODDS`, no per-map scale-up, no player burst-upgrade. Spec §4/§6 wants a tap to throw a **burst** (base odds × a free per-map step-up × a paid upgrade, 1 energy each), and coins to have a **real sink** (today net-zero cosmetics — the "coins have no power" tension).
- **Type:**           follow-up (of T17 `df23351` / T19) — pulls the burst mechanic + the **burst-upgrade spend path** forward from the parked KEYSTONE economy item (owner 2b).
- **LLM-reliability:** **mixed.** The mechanic + sink LOGIC (burst sizing, cost ladder, persist, board loop) = **high** — deterministic, headless-verifiable. The economy IMPACT = **low** — sim-checked + an owner pacing call (surfaced).
- **Human-in-loop:**  **recommended** — two owner calls up front (1a defer-faucet · 2b build-the-sink), and **one surfaced mid-task**: the sim re-validation found burst **front-loads energy spend** → I2 tripped on 1/10 seeds (map 2, 0.32 vs 0.30); owner chose **log-to-parked-pass + early-map (1–2) I2 → WARN, hard-check maps 3+**.

  **Scope (in):** engine-only (owner's "base-engine-specific, nothing in grove" constraint) — `content.gd` `burst_count`/`burst_upgrade_cost`/`burst_upgrade_max` + re-exports; `board.gd` the burst LOOP in `_pop_seed` (N items, each 1 energy, bounded by water + open cells; `roll_spawn` per item with `empties` shrunk so each lands in its own cell) + `_gen_burst_level`/`_upgrade_gen_burst` (the coin sink — `Save.spend`, persisted `burst_lvl`, capped). Grove `grove_data.gd` supplies only the 4 dials (`BURST_ODDS`/`BURST_MAP_EVERY`/`BURST_MAX`/`BURST_UPGRADE_COSTS`). Sim `grove_sim.gd`: burst-modelled pop + the burst-upgrade sink in the bot + the Z report + the early-map I2 exemption.
  **Scope (parked):** the **+50 gift + 1/day refill** faucet changes → the parked "§7 economy tuning + pacing sign-off" item (owner 1a); the burst-upgrade **trigger UI** → the parked KEYSTONE hub-upgrade surface (the spend-path LOGIC is built + sim-modelled; the surface need only wire a buy affordance).
  **Decisions:** (1) **engine-only** — all logic in `engine/`, grove supplies data dials. (2) burst = `BURST_ODDS` base (1/2/3) + a free per-map step-up (every `BURST_MAP_EVERY`) + the paid upgrade, clamped `[1, BURST_MAX]`; each item still costs 1 energy. (3) the burst-upgrade is **the coin sink** (`BURST_UPGRADE_COSTS` 60/180/480 ladder, `-1` past max). (4) burst-induced I2 overage → logged to the parked pacing pass; sim treats early maps 1–2 as WARN (was map-1-only), hard-checks steady-state maps 3+.
- **Verification:**   **DONE.** TDD (RED→GREEN): wrote the mechanics burst tests + the board burst/upgrade test FIRST (RED — `burst_count` then `_gen_burst_level` "Nonexistent function"), then implemented to green. Evidence — `mechanics_tests` **47/47** (map-1 base 1–3 · later maps bigger · upgrade raises · capped at `BURST_MAX` · floored at 1 · cost ladder escalates + caps), `grove_tests` **301/301** (the burst+upgrade board test: a tap throws ≥3 items at upgrade-lvl 2, coins spent, level persists across scenes, sink caps; + 3 single-pop tests updated to the burst contract — water charged per burst item), `make test` **456/456**, `make smoke` OK. **Sim** (`grove_sim`, burst-modelled) **PASS across 10 seeds**: I1 no-jam · no-strand · Y selling-not-income; **Z now absorbs 100%+** of the coin faucet (burst sink ~720🪙 + waysides) vs ~60% before — the first real coin sink; I2 steady-state (maps 3+) green, early maps 1–2 WARN (burst front-loads spend — logged to the parked pass; confirmed against the pre-burst baseline where all seeds passed).
- **Iterations:**     2 GREEN passes — the first surfaced 4 collateral failures: a `fresh("burst")` inserted mid-section-11 switched the save out from under `s2`'s coin tests (relocated the burst block to the section end); 3 neighbour tests asserting the old one-item-per-tap contract (made the water-cost / FTUE / ftue-OFF asserts delta-based; widened the W2 rapid-tap open region so 5 bursts can't fill the board, asserted ≥5). Then the sim I2 overage surfaced → owner-decided.
- **Result:**         **DONE, 2026-06-15.** A generator tap now throws a **burst** (map- and upgrade-scaled), each item 1 energy; the **burst-upgrade is a live, sim-modelled coin sink** (Z faucet now fully absorbed — closes the "coins have no power" gap for v1). Engine-only; grove supplies the four dials. Committed on **`feat/burst-pop`**; **merge to `main` HELD** (owner call, 2026-06-15) — main carries a large uncommitted in-flight rework (the "one image = one map" core-loop work: a 1096-line `map.gd` rewrite + grove_spec + tool deletions) overlapping T21 on 8 files, so T21 stays on its isolated branch to merge cleanly once that work lands. Parked: the +50/1-day faucet (→ §7 pacing pass, owner 1a) + the burst-upgrade trigger UI (→ KEYSTONE hub surface; logic built). BACKLOG updated — burst item removed, its remaining parts folded into the parked tuning + KEYSTONE items. **— UPDATE 2026-06-15:** the hold is lifted — merged to `main` via `04d9e1d`, renumbered T21→T23; the sim's `wayside_sink_capacity()` ref dropped (waysides retired by T21) so the Z report measures only the burst sink.

### T24 — Level-gated obstacle cells (§4: tier-ring → per-cell min_level)   ·   2026-06-15   ·   mechanics   ·   done   ·   *(renumbered T21→T24 at merge — parallel-thread collision; map-model is the entrenched T21, hub-yield→T22, burst-pop→T23)*
- **Asked:**          "review the backlog item, it may be somewhat outdated, let's fix the generator code, give me the steps to fix those [Level-gated obstacle cells]." Then: "proceed in a new worktree."
- **Problem:**        §4 reworked obstacle gating to a per-cell **`min_level`** (hand-tuned diamond — L2/L3 frontier → L12 corners) that unseals in waves as the player levels, still opening on an adjacent merge. Code is **tier-ring only**: `bramble_gate` encodes `gate_line*16+req_tier` keyed off Chebyshev `ring_of` (`content.gd:198`), opened when an adjacent merge **produces the required tier/line** (`openable_brambles`, `board_model.gd:161`) — no `min_level`, no player-level input. Backlog anchors stale (`content.gd:90`/`board_model.gd:114` predate the `core/` split). The "open sub-Q" (merge-openable vs auto-open) is ALREADY resolved in spec §4 L164 = **merge-openable** (no Dev call).
- **Type:**           new (reworks the §4 obstacle field that shipped pre-T17; the tier-ring model was the prior design, so not a regression).
- **LLM-reliability:** **mixed.** The mechanic (data table, level-gated openable check, call-site + view + test rewrites) = **high** — deterministic, headless-verifiable. The **no-strand validity** of a level-gated board = **low** — at L1 the player is boxed to the 9-cell center until L2 (the old model grew on the first t2 merge with no level req); whether that strands needs the Monte-Carlo sim across seeds, not an eyeball, and the gradient's *feel* is an owner tune the sim can't make.
- **Human-in-loop:** **recommended** — the sim gates no-strand, but the gradient feel (how tight the early board should be) is an owner's-eye sign-off. Same low-reliability balance family as T19 + the burst-pop item.
- **Reconcile at pickup (2026-06-15):** baseline `grove_tests` **291/291** green in a fresh worktree off HEAD (bc648cc — includes T18–T20). Working in worktree `feat/level-gated-cells` — a **Dev-approved one-off exception** to the standing no-worktree rule, to isolate from other agents' uncommitted main-tree edits (`content.gd`/`grove_data.gd`/`grove_sim.gd`, the collision flagged at review). **Encoding decision (to record in spec §4):** keep `terrain` a `0=open / ≠0=sealed` flag; the gate reads the static `MIN_LEVEL` table via `cell_min_level`, **never the stored terrain value** → old saves' tier-encoded values still read as "sealed", so **no save migration**.
- **Scope:** (1) `grove_data.MIN_LEVEL` 9×7 table (spec §4 verbatim) · (2) `content.gd` bramble field → `cell_min_level`/`open_at_start`, drop `bramble_gate`, positional `bramble_contents` · (3) `board_model.openable_brambles(cell, level)`, drop `gate_line_of`/`gate_req_of` · (4) `board.gd` merge-trigger passes `_quest_level()` + `_make_bramble` art band off `cell_min_level` · (5) `grove_sim` 3 call sites + re-run no-strand across seeds · (6) `grove_tests` two bramble blocks rewritten · (7) docs (spec §4 flip, backlog → done, this entry).
- **Verification (mechanic — DONE):** `make test` **436/436** (incl. the layering guard 29/29 — `cell_min_level` is core, called *downward* by scenes/sim); the rewritten grove bramble blocks assert the level gate (a cell stays sealed under its min_level, any adjacent merge opens it at/above it, contents revealed). **No-strand sim PASS** across seeds 42/7/123/999 at both 14d and 30d (I1 zero jams · no-strand). Committed `f64ecc0` on `feat/level-gated-cells`.
- **⚠️ Surfaced — owner pacing call (the LOW-reliability half):** an A/B sim (old tier-ring `bc648cc` vs new, 30d, same seeds) shows level-gating **~halves progression** — stars earned seed42 154→89, seed7 181→80; maps 4–5/5 → 2/5; level 11–12 → 7–8 — and **caps the FTUE board at 9 cells / 2 free until L2** (at L1 nothing is openable, so the old "first merge → board grows" loop is dead until ~6★ are earned in 2 cells). No-strand holds (bag + merchant valves cope), so it's a FEEL/pace call, not a strand. **Root cause:** the spec's inner frontier is L2 but the player starts at L1 → a chicken-and-egg throttle the spec's "L2 is reached early" assumption missed (this sim is exactly what "pending sim re-validation" was for). **Recommendation:** open the inner ring at **L1** (restore the immediate first-frontier) and pull the early bands down, keeping the outer corners as the level-gated tail; iterate the softer table against the sim back to ~old star-rate. **Merge HELD** pending the Dev's gradient sign-off.
- **Result:**         **DONE (mechanic), 2026-06-15** — code `f64ecc0` · docs `9691430` + this finalize commit. Owner's call: **ship spec-faithful, PARK the tune.** Spec §4 flipped to "in code" + the provisional-gradient finding recorded; the backlog item is marked done and the gradient softening folded into the **Economy tuning + pacing sign-off** item (tune the gradient **jointly** with `LEVEL_STARS`/`LEVEL_WATER_GIFT` vs no-strand AND I2, after burst-pop settles the water faucet). **Merge to `main` PENDING the tree clearing** — the primary tree carries other agents' uncommitted edits across all 6 of this task's files, so a clean `--no-ff` merge waits on them committing. Verified + signed-off on the branch. **— UPDATE 2026-06-15:** the tree cleared — merged to `main` via `ab7e23f`, renumbered T21→T24; grove bramble tests rewritten tier-ring→min_level (grove 267/0), no-strand holds under level-gating + burst in the sim.

### T25 — Generator burst-upgrade: buy UI + the cap-decouple/re-price (the §6 coin sink)   ·   2026-06-15   ·   mechanics   ·   done
- **Asked:**          (keystone P0 pasted) "review the backlog item, it may be somewhat outdated, let's fix the generator code, give me the steps to fix those … ignore the ongoing operations from other agents, just focus on what needs to be revamped in current code base." → focus: the **generator** code (Part B).
- **Problem:**        the §6 burst-upgrade **coin sink** (T23) was BUILT but **unreachable** — `_upgrade_gen_burst()`/`_gen_burst_level()` were called **only by tests**; no board/hub/HUD surface triggered them, so players could never spend coins on it. The whole §6 board sink was dead in production (the "coins have no power" gap Part B was meant to close). Backlog said wire the trigger UI "on the **hub** surface"; spec §8 says the burst-upgrade is "board-level… **independent of the hub**" — a contradiction that decides where the UI goes.
- **Type:**           follow-up (of T23 `04d9e1d`) — wires the trigger UI parked by T23 ("the spend-path LOGIC is built; the surface need only wire a buy affordance").
- **LLM-reliability:** **mixed.** The buy LOGIC (spend/refuse-broke/refuse-maxed, chip rebuild + label, persist via the existing `_upgrade_gen_burst`) = **high** — deterministic, headless-testable. The chip's **look + placement** = **low** — perceptual; composited-verified, not eyeballed.
- **Human-in-loop:**  **recommended.** Two owner calls, both answered: (1) the placement contradiction (board vs hub) → **on the board (generator chip)**, spec-§8-aligned; (2) the **cap-decouple + re-price** → owner "do this now" (the re-price values are an economy-feel call, decided against the sim's absorption read). The pill's **final pixel placement** (it juts slightly into the empty cell below the generator) remains an owner feel-tune.
- **Reconcile at pickup (2026-06-15):** verified **T21/T22(A1)/T23/T24 are ALL merged to `main`** (`c0a3acd`) — `git merge-base --is-ancestor` true for all three worktree branches; the `.claude/worktrees/*` are **stale leftovers** (their work integrated, cleanup skipped). So the backlog item's "code in progress · A2–A9 queued behind T21" was **stale** (T21 merged → A2–A9 unblocked); anchors `_on_spot_tap ~:957`/`spot_owned :181` also stale → real `:654`/`:158`. Refreshed the item. Left the worktrees untouched (Dev: "ignore the ongoing operations from other agents").
- **Scope — beat 1 (buy UI):** `board.gd` — `_rebuild_burst_chip` (a `Look.stat_chip` pill anchored under the primary generator, `MOUSE_FILTER_STOP` so its tap never pops the gen under it; centered via `get_combined_minimum_size`), `_on_burst_chip_input` (tap → buy), `_refresh_burst_chip` (label `Burst L{n} ▸ {cost}` / `L{n} ✦` at max), `_try_buy_burst` (calls the existing `_upgrade_gen_burst`, wobble on broke/maxed); wired into `_rebuild_all`. `grove_tests` +6 (the pill path). **Decision:** the burst level is **global** (one `burst_lvl` for all generators, per §8 board-level) → **one** pill on the primary generator, per-generator burst parked.
- **Scope — beat 2 (cap-decouple + re-price, owner "do this now"):** the free per-map scale-up was eating the paid headroom under `BURST_MAX=6` (top paid level bought ~nothing on maps 4–5). Fix: `content.gd` `burst_count` rewritten — the FREE portion (base + per-map gift) caps **on its own** at `BURST_FREE_MAX`, the paid level adds **on top**; `grove_data.gd` dials `BURST_FREE_MAX=4`, `BURST_MAX` 6→8, ladder re-priced + lengthened `60/180/480` → **`120/360/840/1800`** (4 levels, total 3120 > faucet). `mechanics_tests` +3 (the decouple regression guard). `grove_sim` re-run.
- **Verification:**   **DONE.** `make test` **433/0** (save 32 · layout 17 · mechanics **50** · layering 29 · quest 32 · grove **273**). Beat 1 — +6 pill assertions (renders when a generator is present · a broke tap refused with **no level + no debt** · a funded tap raises the level + spends the exact ladder cost · driven to max it refuses with **no overspend**); **chip composited-verified** on the live board via `grove_shot.gd` (real renderer) — a legible "🪙 Burst L0 ▸ 60" pill centered under the "seeds" generator (`/tmp/burst_chip_z2.png`, 3× crop). Beat 2 — +3 decouple regression asserts (deep-map FREE burst caps at `BURST_FREE_MAX` · **each paid level adds +1 on top of the free cap** · the top level reaches `BURST_MAX`); **`grove_sim` re-validated** (seeds 42/7/999, 30d): no-strand · I2 steady-state hold, and the **burst sink now absorbs 64–76% of the coin faucet** (was 34–50%; ladder total 3120 > faucet so coins keep a target). No regression.
- **Iterations:**     3 — (1) a parse error: `var tap := (… ev.pressed)` couldn't infer bool from `.pressed` on a base `InputEvent` → mirrored the existing `var pressed: bool = …` pattern (board.gd:1491). (2) placement: first pass was left-aligned + jutted asymmetrically into the cell below → recentred with the computed pill width. (3) re-price: first ladder `[80,240,640,1500]` only lifted absorption to 44–53% (bot reaches L3, the re-price barely moved the sink); raised the reachable levels to `[120,360,840,1800]` → 64–76% (burst behaviour unchanged — same level reached — so invariants held, only coins-spent rose). No Dev-caught miss.
- **Result:**         **DONE, 2026-06-15.** The §6 burst-upgrade coin sink is **reachable AND well-valued** — an on-board buy pill (board-level, every map) + a decoupled cost ladder where every paid level always adds +1 and the sink absorbs the majority of the coin faucet. Closes Part B. Edited `board.gd` · `content.gd` · `grove_data.gd` · `mechanics_tests.gd` · `grove_tests.gd`; docs: `BACKLOG.md` (Part B + burst-tuning done, status/anchor refresh, seed-123 finding), `merge_spec.md` §6 (board-level/global pill + decouple resolved). **⚠️ SURFACED:** the integrated `main` **jams on seed 123** (burst × level-gating, map 1) — pre-existing (each branch passed alone; the parallel-merge reconcile never re-ran the sim on combined `main`), **neutral to this change** (map-1 burst is unchanged by the decouple). Logged to the Economy-tuning item as the strongest case for opening the inner ring at L1. Parked: per-generator burst; the stale-worktree cleanup. Committed + pushed (per Dev instruction).

### T37 — Seed-123 strand: open the L1 board frontier (level-gating × burst jam)   ·   2026-06-15   ·   economy   ·   done (pending owner feel sign-off)
- **Asked:**          (after the batch merge) "clean up all merged branch, update backlog, then **work on the seed-123 jam in a separate work tree**."
- **Problem:**        the `MIN_LEVEL` diamond opens **zero cells at L1** (everything outside the center 3×3 is min_level ≥ 2; L2 needs 6★, `LEVEL_STARS[1]`). So the early board is boxed to ~9 cells until L2 — and on unlucky seeds (123) the cramped board **jams** (fills, no merge, no delivery) before earning the 6★ for L2: a chicken-and-egg strand (need L2 to grow the board, need a bigger board to earn L2's ★). Burst (T23) worsens it by flooding the small board. This is the T24-surfaced level-gating tension (parked in the Economy-tuning item), now a HARD strand in the integrated `main` (90 jams/30d on seed 123, faucet 0).
- **Type:**           follow-up (of T24 level-gating × T23 burst, integration strand) — pulls the parked "open the inner ring at L1 + joint gradient tune" from the Economy-tuning item.
- **LLM-reliability:** **low** — economy/pacing FEEL. The **no-strand fix is sim-verifiable** (objective: seed 123 must stop jamming, all seeds no-strand). But the **gradient feel** (how fast the board opens) + the **I2 balance** (water gift vs spend) is an owner taste call — build the check (Monte-Carlo sim across seeds), present the feel for sign-off.
- **Human-in-loop:**  **recommended** — the gradient feel + any `LEVEL_STARS`/`LEVEL_WATER_GIFT` rebalance is owner taste; same low-reliability family as T19/T23/T24. The strand-fix itself (board must not jam) is objective.
- **Plan:**           (1) open the inner ring at L1 (`MIN_LEVEL` frontier → 1, merge-openable from the start) so the board grows on the first merges; (2) re-validate on `grove_sim` — seed 123 must not jam, seeds 42/7/999 no-strand + I2 steady-state (maps 3+); (3) if early leveling over-feeds the water gift, tune `LEVEL_WATER_GIFT`/`LEVEL_STARS` jointly; (4) verify `make test` green (the T24 bramble tests assert the gate). Worktree `worktree-seed123-jam`.
- **Decision (gradient shape):** tried (a) minimal — only the inner ring 2/3→1 — sim-clean but it left a **1→4 gap** (no L2/L3 cells) that read worse and broke the tests' "below-gate" fixtures; chose (b) a **uniform shift of the whole diamond down by 1** (center stays 0; corners 12→11) — smooth 0→1→2→3→5→…, opens the inner N/S frontier at L1, keeps L2/L3 cells adjacent to center. `LEVEL_WATER_GIFT`/`LEVEL_STARS` left untouched (the shift alone fixed the strand within the sim's invariants; touching the gift conflicts with the parked +50 idea).
- **Verification:**   **strand FIXED, sim + suite green.** `grove_sim`: seed 123 **90 jams → 0** (now completes 40/40 spots · 5 gates · L12), and a **14-seed sweep (123/42/7/999 + 10 more) = 0 FAILs** — no-strand + I2 steady-state (maps 3+) hold on all. `make test` **657/0** across 17 suites; `make smoke` clean. The `hint_tests` + `grove_tests` bramble blocks were rewritten **gradient-agnostic** (derive from `cell_min_level`, assert the open-at-gate / blocked-below-gate mechanism) so this and future re-tunes can't break them. ⚠️ **side-effect for owner:** the faster early opening lifts the **map-1 water-gift ratio** (~0.37 → ~0.8–1.5 across seeds) — still WARN-exempt (the sim treats onboarding maps 1–2 as WARN, hard-checks maps 3+), but map 1 now mildly self-sustains during onboarding; tune `LEVEL_WATER_GIFT` down if that crosses the "never self-sustains" line for you.
- **Iterations:**     2 gradient versions (minimal → gap/test-breakage → uniform shift-by-1). No Dev-caught miss yet (pending sign-off).
- **Result:**         **DONE + verified, NOT merged — pending owner feel sign-off.** The strand fix is objective (sim + tests green); the gradient *feel* + the map-1 ratio shift are the owner's dial. Committed on `worktree-seed123-jam` (`grove_data.gd` MIN_LEVEL + comment; `grove_tests.gd`/`hint_tests.gd` gradient-agnostic; this entry). Merge to `main` on the owner's OK (or after a gift/gradient adjust).

### T39 — Selling: per-map coin bands + drag-only verb (§6/§9)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (economy backlog batch) "identify the Open—economy items unblocked + parallelizable now; dispatch one worktree each, merge + clean up, update the backlog." Selling = one of three (with T40 Shop, T41 Bag).
- **Problem:**        sells paid flat tier-coins with **no per-map band**, so later maps weren't an economic step-up (§6); **tap-sell still existed** against §9's drag-only rule; the stall pill showed a stale flat `MERCHANT_COINS=25`.
- **Type:**           new (closes the §6/§9 selling backlog item).
- **LLM-reliability:** **high** — deterministic + self-verifiable: the band is a data table, and the 32× anti-arbitrage proof + `t8 = flat 1💎` are test/sim invariants. (The band *values* are a feel dial, not a correctness risk.)
- **Human-in-loop:**  **recommended** for the band VALUES only; **none** for correctness.
- **Verification:**   `grove_tests` +15 asserts — band size == map count + monotonic ≥1.0; `map_for_line`/`map_for_code` resolve across maps 0–4; every t1–t7 reward == `round(tier×band)`; **t8 == exactly 1💎 on every map** (the pinnacle); tap-sell (`_on_merchant_tap`) absent; pill reads `+1💎`. Economy sim PASS — selling-not-income tripwire **8.8 < 25**, 32× round-trip holds (earn 1💎 = 128💧 vs buy = 4💧), 0 jams. After merge: grove **344/0**, all 17 engine suites 0-failed (`make test`).
- **Iterations:**     1 — built fire-and-forget in worktree `t39-selling`; merged `--no-ff` with **zero conflicts** (regions stayed disjoint from T41's board.gd/grove_data.gd edits).
- **Result:**         **DONE.** Per-map band `[1.0, 1.3, 1.7, 2.2, 2.8]` (maps 1–5, owner-tunable feel dial); new `content.map_for_line`/`map_for_code` + `sell_map_band`; `MERCHANT_COINS` retired. Agent commit `9b16766`, merged into `main` (economy batch 1/3). Files: `core/content.gd`, `grove_data.gd`, `scenes/board.gd`, `grove_tests.gd`.

### T40 — Shop: item-shortcuts + cosmetics + rotating offers (§10)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (same batch) the §10 Shop buy-side-sinks item.
- **Problem:**        the Shop was a **fixed static** layout (water + coin pouch + 3 cash packs) — no rotation, no item-shortcuts, no cosmetics; a stale "no IAP wired" header contradicted the live confirm-grant cash packs.
- **Type:**           new (closes the §10 Shop backlog item, buy-side scope).
- **LLM-reliability:** **high** — pure grant funcs + a **deterministic (day-seeded) rotation**, all test-covered; only the storefront layout is perceptual.
- **Human-in-loop:**  **recommended** for stock / prices / rotation-count (feel dials); **none** for correctness.
- **Verification:**   `grove_tests` +42 asserts — item-shortcut grant (spends exact coins/gems, refuses when broke, queues the piece, board drains it to the bag on open), gem-shortcut spends 💎-only, cosmetic grant (spends, unlocks, **own-once** no double-charge), **rotation determinism** (same seed → identical offers no dupes; advancing the seed rotates), Featured-band UI shape. Layering guard green (scenes→ui→core). After merge: full `make test` green.
- **Iterations:**     1 — fire-and-forget in worktree `t40-shop`; merged `--no-ff` with **zero conflicts** (the 4 additive board.gd hooks auto-merged alongside T41's bag block — the predicted ~1750–1923 overlap resolved textually).
- **Result:**         **DONE.** New grove **§10 SHOP STOCK** — 5 item-shortcuts (t3–t5 from already-askable lines, coins-low/💎-high → a grind-skip, never a gate), 4 cosmetics, **rotation = 3 of a 9-offer pool**. Stale header corrected (cash packs are live confirm-grant). Agent commit `4f15a1a`, merged into `main` (batch 3/3). Files: `ui/shop.gd`, `grove_data.gd`, `scenes/board.gd`, `core/tuning.gd`, `grove_tests.gd`, `tools/map_shot.gd`. **PARKED:** the cosmetic LOOK is owned-but-not-yet-applied to the board render (wire the theme next); the live-IAP price ladder + starter pack + piggy bank stay on the (blocked) Rewarded-ads / Monetization items.

### T41 — Bag capacity: 6→18 slot model + drag-back retrieve (§5)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (same batch) the §5 Bag capacity-model item.
- **Problem:**        the bag was the OLD `BAG_SLOTS=2` + single `bag3` (10💎) model with a hard-coded 3-slot UI and **tap-retrieve** — not §5's **6→18** premium-expansion model with **drag-back**.
- **Type:**           new (closes the §5 Bag backlog item).
- **LLM-reliability:** **high** for the model/save (deterministic, migration-tested); the **drag-back FEEL is eyeball-only** (headless can't render the drag preview / snap-back).
- **Human-in-loop:**  **recommended** — the per-slot 💎 schedule (210💎 total) is a premium-pressure feel call **no sim covers** (the sim doesn't model the bag); the drag-back feel needs an on-device eye.
- **Verification:**   `save_tests` +10 — default 6; buy → +1 & 💎 spent; broke refused; the 12th expansion reaches 18; **buy at cap refused & no 💎 charged**; `set_bag_slots` clamps both ends; persists across reload; **migration from a legacy `bag3=true` save keeps bagged contents intact**. `grove_tests` rewrote the old 2-slot block → 6-owned + buy-slot (grow/refuse/cap-drops-the-affordance) + **drag-back `_retrieve_from_bag` places + empties the slot**. `make smoke` clean (live UI+board). After merge: full `make test` green.
- **Iterations:**     1 — fire-and-forget in worktree `t41-bag`; merged `--no-ff` with **zero conflicts**.
- **Result:**         **DONE.** 6 start / 18 max; `BAG_SLOT_PRICES = [10×3, 15×3, 20×3, 25×3]` (escalating bands, owner-tunable); `save.gd` `bag_slots`/`set_bag_slots`/`buy_bag_slot` + legacy migration; drag-back via a `_bag_drag_idx` gate (board-piece drag untouched). Agent commit `dfd70e4`, merged into `main` (batch 2/3). Files: `core/save.gd`, `core/content.gd`, `core/board_logic.gd`, `scenes/board.gd`, `grove_data.gd`, `save_tests.gd`, `grove_tests.gd`, `grove_spec.md` (§5). **PARKED:** drag-back UX on-device eyeball; an optional confirm on the slot-buy 💎 spend.

### T42 — Home-hub yield + upgrade-levels (the KEYSTONE, Part A · A2–A9) (§8/§10)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (2nd economy batch) "continue with the rest" — the ⭐P0 keystone hub coin loop, in a worktree, with two siblings (T43/T44).
- **Problem:**        hub spots were binary owned/unowned; coins had no real power (§10). Part A makes a restored spot **L1→L5** (richer look + higher yield), yield buildings **accrue to a per-building daily cap**, swept in **one collect-on-return beat**.
- **Type:**           new (closes §8/§10 keystone Part A; Part B was T23/T25).
- **LLM-reliability:** **split** — the accrual/upgrade MATH is high (deterministic, tested); the yield/cap/upgrade-cost **PACING is low** (economy feel — needs the sim + owner sign-off, the T19/T37 family).
- **Human-in-loop:**  **recommended** — the pacing dials are an owner feel call; the "extend-never-self-sustain" invariant is sim-checkable (objective).
- **Verification:**   `grove_tests` +37 (accrual = rate×elapsed capped; collect sums + resets the clock; décor/unrestored = 0; upgrade raises level + next rate; refused at max/broke; restore→L1). `grove_sim` EXTENDED with the hub faucet + hub-upgrade sink: all invariants PASS incl. **§8 "a week of max yield (672🪙) < the standing burst sink (3120🪙) — extend, never self-sustain"**. `make smoke` clean. After the batch merge: grove **455/0**, all 16 engine suites 0-failed.
- **Iterations:**     1 — the agent CUT the rates from a first draft (240🪙/day) after the sim showed they out-faucet the sinks → final **96🪙/day** max. No Dev-caught miss (pending feel sign-off).
- **Result:**         **DONE + merged, pacing PENDING OWNER FEEL SIGN-OFF.** Restore→L1, upgrade L1→L5; `HUB_YIELD_RATE`/`HUB_YIELD_CAP`/`HUB_UPGRADE_COST` in `grove_data` (owner-tunable). The home-shortcut shows a **yield-ready pip**. Agent commit `a0cef68`, merged `--no-ff` (economy batch 1/3). Files: `core/content.gd`, `scenes/map.gd`, `ui/hud.gd`, `grove_data.gd`, `grove_tests.gd`, `tools/grove_sim.gd`, `grove_spec.md`. **PARKED:** per-map yield + cross-map feed-forward.

### T43 — Live-IAP ladder + starter pack + rewarded ads + out-of-water offer (§4/§10)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (same batch) the §10 rewarded-ads + live-IAP surfaces + the triggered out-of-water offer.
- **Problem:**        only a 3-tier placeholder cash set + a stale "no IAP" header; no rewarded ads, no starter pack, no contextual offer at the energy wall (§4 socket).
- **Type:**           new (closes the Rewarded-ads/live-IAP item + the triggered-offer half of Monetization).
- **LLM-reliability:** **high** — pure grant/cap/cooldown funcs, deterministic + tested. The ad/IAP SDK itself is external (stubbed behind the honest confirm).
- **Human-in-loop:**  **recommended** for the pack ladder / ad caps / offer discount (feel dials).
- **Verification:**   `grove_tests` +66 (order T): ladder well-formed + rising 💎/$ + $49.99/$99.99 ceiling; each tier grants its 💎; first-buy doubler once-then-stops; starter once + water credit; ad grants then cooldown/cap-refused + next-day reset; OOW offer fires within cap then refused. Layering guard +2 (`core/ads.gd` imports no ui/scenes). `make test` 804/0 in isolation.
- **Iterations:**     1; merged `--no-ff` (the `grove_tests` runner-call conflict resolved keep-both; everything else auto-merged).
- **Result:**         **DONE.** 6-tier ladder ($0.99–$99.99, rising 💎/$), $1.99 starter + first-buy ×2, `core/ads.gd` (refill / 2×-collect / reroll / event-topup, capped + cooled), a cozy out-of-water offer at the wall. Agent commit `dac167a`, merged `--no-ff` (batch 3/3). Files: `ui/shop.gd`, `scenes/board.gd` (refill area), `core/ads.gd` (new), `core/save.gd` (block), `grove_data.gd`, `core/tuning.gd`, `grove_tests.gd`. **EXTERNAL REMAINDER:** the real ad-SDK + IAP receipt validation behind the geo flag (the one piece that can't ship in-engine; stubbed). The `Ads.collect_2x` hook it exposed is wired into the hub-collect by **T45**.

### T44 — Piggy-bank accrual vault + forgiving daily login calendar (§10/§18)   ·   2026-06-15   ·   economy   ·   done
- **Asked:**          (same batch) the §10 piggy vault + the §18 daily login calendar.
- **Problem:**        no accrual vault, no login calendar; the `save.daily()` streak system was unwired AND did a punitive day-1 reset (un-cozy, §18).
- **Type:**           new (closes the vault + login halves of Monetization).
- **LLM-reliability:** **high** — skim/crack + ladder/claim math deterministic + tested; the §13 surface LOOK is perceptual.
- **Human-in-loop:**  **recommended** — skim rate / pig price / streak ladder feel dials.
- **Verification:**   `save_tests` +26 (skim floor + carried remainder + accrual; crack grant+reset+persist; ladder escalates; day-7 milestone; water-safe cap; claim-once; **FORGIVING decay 5→4 on a 1-day gap, never 0**). `grove_tests` +8 (skim wiring + both surfaces build framed + crack/collect driven end-to-end through the real buttons). layering +6 (vault/login are `core/`, no ui import). `make smoke` clean.
- **Iterations:**     1; merged `--no-ff` (`grove_data` + `grove_tests` conflicts resolved keep-both; the 3 skim-sites + `save.gd` auto-merged).
- **Result:**         **DONE.** `core/vault.gd` (25% skim w/ carried remainder, crack at $2.99, min 30💎) + `core/login.gd` (escalating week ladder, day 7/30 milestones) on a now-**FORGIVING** `save.daily()`; diegetic `ui/vault.gd` jar + `ui/login.gd` calendar. Agent commit `482122c`, merged `--no-ff`. 3 flagged `SKIM-SITE` one-liners in `content.gd` (level-up) / `map.gd` (map-restore) / `board.gd` (t8-sell). Surfaces wired to entry points by **T45**.

### T45 — Wire the monetization entry points (2×-collect ad · piggy vault · login popup)   ·   2026-06-15   ·   economy   ·   done-on-branch (NOT merged — HELD)
- **Asked:**          (dispatcher integration) make the T42/T43/T44 engines REACHABLE — the parallel agents left the cross-feature hooks + entry points unwired (each owned only its files).
- **Problem:**        T42's hub-collect never read T43's `Ads.collect_multiplier` (the 2× ad doubled nothing); T44's vault + login surfaces had **no entry points** (built + tested but dead in play).
- **Type:**           follow-up (integration of T42/T43/T44).
- **LLM-reliability:** **high** for the wiring/reachability (tested); the **UI PLACEMENT is low** (perceptual — needs the owner's eye).
- **Human-in-loop:**  **recommended** — the piggy-button position, the 2× card look, and the login-popup timing are perceptual placement calls.
- **Verification:**   `grove_tests` +21 (the post-collect 2× doubler credits a second `got` + consumes the arm; no offer when the ad is capped; piggy pip dark sub-threshold + lit when claimable; the piggy button opens the jar; the login popup fires on a fresh unclaimed day, stays shut when claimed, skips the cold FTUE). `make smoke` clean. `make shot-map` confirms the hub 2×-offer + piggy button + login popup render.
- **Iterations:**     1.
- **Result:**         **DONE + VERIFIED on branch `t45-integration` (commit `90a8ff0`) — NOT merged, HELD.** The primary tree has **active uncommitted §16 map-art/placement work** touching `map.gd` (a stable-canvas/placed-art rework + `layout.gd` + a re-import + new `furn_fh_*`/`map_farmhouse` sprites), and T45 also edits `map.gd` — merging would fight that WIP, so the merge was aborted cleanly (the WIP is untouched). **Merge after the §16 map work commits, reconciling the `map.gd` overlap.** Resolves the 2×-collect as a POST-collect "double it" offer (collection is auto-on-entry). New `daily_login_popup` flag. Files: `scenes/map.gd`, `core/features.gd`, `grove_tests.gd`, `tools/map_shot.gd`. **PERCEPTUAL placement flagged for owner sign-off.**

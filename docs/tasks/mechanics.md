# Mechanics & Systems — task log

Rules & loops: merging, generators, quests, progression, economy logic, gates, win/lose.

_Cleared for a fresh start (2026-06-14). New entries below; see docs/TASKS.md for the format._

### T17 — Per-zone generators + merge-to-evolve (engine mechanic)   ·   2026-06-14   ·   mechanics   ·   done
- **Asked:**          "split [the per-zone-generators epic], use placeholder for art, focus on engine."
- **Problem:**        generators today **accumulate per-chapter and are never consumed** (`active_gen_indices(chapter)` content.gd:61; `set_active_gens` only adds, board_model.gd:53), **can't be moved** (board.gd:1513), and there is **no evolve-merge / line-retirement**. Spec §6 wants a **per-zone roster** (≤4 live, **2 lines each**) where a granted generator **merges onto an old one** (old consumed, old lines retire).
- **Type:**           new — foundation for the parked grove-map + art tasks; live gate-quest wiring depends on §7 (quests).
- **LLM-reliability:** high — pure, deterministic board/roster logic, fully self-verifiable headless. (Art is the low-reliability piece — parked.)
- **Human-in-loop:**  none for the mechanic; recommended later for the congestion-sim feel call (z4–5 = 8 live lines) and the grove content map.

  **Scope (in):** new per-zone generator data SHAPE (`zone` + 2 `lines` + `evolves_from`); roster-derivation logic (live set / live lines / retired lines / evolve lineage); the evolve-merge board op; line-retirement bookkeeping; generators as movable/swappable pieces (never sold, never normal-merge); save-schema slice (live set + retired lines) with a v3 migration; headless tests; sim + grove placeholder-roster kept runnable (placeholder/code-drawn art).
  **Scope (parked):** the real grove 16-gen/32-line themed map + lineage + names (content task); ~256 item + 16 gen sprites (art task); the authored t8 gate-quest + generator-grant quests (lands with §7); the Collection archive of retired lines (its own backlog item — this task only *flags* a line as retired).
  **Decisions (recorded here; player-facing ones → spec §6 note):** (1) roster is a flat data array, engine derives per-zone sets — keeps the 16/32 roster as data. (2) Retirement = stop generating + stop asking that line; **items already on the board stay** (mergeable/sellable), no sweep, no loss; Collection hook is parked. (3) **Interim, until §7:** generators are granted on **zone entry** and the evolve-merge op is exposed for §7 to drive via the grant quests — so the live game runs on the new model without the quest rework. (4) `gen` is a movable piece but **refuses sell and normal-merge**; the only gen-merge is grant→target evolve.
  **Acceptance (observable, headless):** roster fns return the right live/retired lines + lineage; evolve consumes the old gen, installs the new at its cell, retires the old's lines; a retired line is never popped/asked; a gen moves + swaps but can't be sold or normal-merged; save round-trips the new fields and migrates a v2 blob; `make test` green; `grove_sim` runs on the new model with 0 jams.
- **Verification:**   **DONE for the engine mechanic** (economy balance split out → backlog; quests = §7, next). Built + verified: per-zone roster + evolve op + retirement + cell/live-set resolver (pure); a **stateful, persisted `gens` map** in `board_model` (cell→id) — **movable generators (#1)** via `move_gen`, the **player-driven evolve ritual (#2)** via `evolve_gen` (drag a grant onto its predecessor → old consumed, new installed, lines retire; never sold / never normal-merged), **save round-trip (#3)** through `to_dict`/`from_dict` + a seed-if-empty load migration; **dead preview helpers removed (#5)**. Evidence: `mechanics_tests` 34/34 (roster + evolve + move + persist), `grove_tests` 304/304 (incl. **12c**: a live board seeds zone 0, moves a generator + re-renders, evolves it in place + re-renders), `layout` 21, `save` 26 — `make test` **385/385**; `make smoke` OK. Board input rewired cell-keyed (`_release_gen` routes tap→pop / drag→move / grant→evolve).
- **Iterations:**     1 build pass after the model TDD; no Dev-caught misses. (The earlier 4 sim tuning passes informed the economy split-out, not this.)
- **Result:**         **DONE (engine mechanic), shipped 2026-06-15.** Generators are per-zone, stateful, persisted, movable, and player-evolvable. **Intentionally left "broken" past the seeded zone (owner: "let it break"):** there is **no in-play grant DELIVERY** — that is §7's authored grant quests (the next task), so a fresh game sits on its seeded zone's generators until §7 wires delivery. **#4 economy rebalance** (sim still I2-red at uniform `t2–4`, no jam / no strand) **split to its own backlog item** (folds into §7's Monte-Carlo pass). `grove_sim` runs (exit 1, economy-red) by design. Recommend **§7 next** (delivers grants + balances the economy). Committed `a7cfa11`, `--no-ff` merged to `main` as `df23351`, pushed to `origin/main`; the four parked pieces (grove map + art + §7 wiring + #4 economy) are all tracked in `docs/BACKLOG.md`.

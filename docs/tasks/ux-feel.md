# UX & Interface + Game Feel & Juice — task log

Readability & layout (HUD, menus, chips, navigation) · and the alive layer: animation, FX, audio, juice.

_Cleared for a fresh start (2026-06-14). New entries below; see docs/TASKS.md for the format._

### T27 — reconcile §12 juice verb table to real call names   ·   2026-06-15   ·   ux   ·   done
- **Asked:** §12 names the juice verbs "called by name," but the engine implements the motions under different names: `wiggle`→`FX.wobble`, `floater`→`FX.floating_text`, `press`→`skin.add_press_juice` (not FX), `hop`/`ambient_bob` in `ambient.gd`. The motions all exist; only the named-verb surface deviates. Reconcile the §12 table to the actual call names (or rename in code).
- **Problem:** Pure spec↔code drift (Tier-3) — the SHIPPED CODE is the reality. The §12 vocabulary table and its intro ("Implemented once in `FX` / `Look` and called by name") were stale: five of ten rows named a verb that no longer matches the callable, and `ambient_bob` named a callable that never existed (the bob is inlined in the ambient wander path). Renaming code would touch many call sites for zero behavior gain, so the fix is to reconcile the doc to the code.
- **Type:** follow-up (doc reconciliation; no behavior change)
- **LLM-reliability:** high — deterministic: every verb maps to a grep-confirmed `static func`/`func` signature in `fx.gd`/`skin.gd`/`ambient.gd` (or, for the bob, to the inlined `Tune.BOB_AMP * sin(...)` line). Self-verifiable by grep; no perceptual judgement.
- **Human-in-loop:** none — mechanical doc↔code reconciliation; no design call.
- **Verification:** each §12 verb now names a grep-confirmed callable in `fx.gd`/`skin.gd`/`ambient.gd` (signatures quoted below); the inlined bob is noted as such rather than as a fake callable; no code changed. Drift fixed: `press`→`Look.add_press_juice` (skin.gd:216), `wiggle`→`FX.wobble` (fx.gd:31), `floater`→`FX.floating_text` (fx.gd:67), `hop`→`Ambient.hop` (ambient.gd:111, callable but not in FX), `ambient_bob`→inlined in `_character_pos` (ambient.gd:69). Already-correct rows kept: `FX.pop_in` (:112), `FX.scatter_in` (:122), `FX.fly_to_wallet` (:154), `FX.tick` (:136), `FX.breathe`/`FX.breathe_once` (:60/:100). Calm-mode prose ("disables `breathe`") left intact — `FX.breathe` exists. Doc-only: `make test` not run (no code touched; a fresh worktree would force a slow asset reimport).
- **Iterations:** 1 — single section-scoped edit to the §12 intro + table; no Dev miss.
- **Result:** done — commit on `feat/t27-spec-juice-verbs` (see merge-back; HEAD at write was `d69cce8`). Files: `docs/design/merge_spec.md` (§12 intro + table), `docs/tasks/ux-feel.md` (this entry). No `.gd` changed. Note for the Dev: line 416 (§13 Shop prose) still says "→ wiggle"; left untouched to keep this edit §12-scoped — park a one-liner if that prose should also track `FX.wobble`.
### T34 — reconcile §13 HUD law to the shipped bottom-bar Shop   ·   2026-06-15   ·   ux   ·   done   ·   *(renumbered from T25 — parallel-thread collision with the shipped burst-upgrade T25)*
- **Asked:**          "HUD law drift — Shop moved out of the top-right bar (decision). §13 says the top bar is 'wallet + Shop' top-right; the code deliberately moved the Shop to the bottom bar. Decide: update §13 to the shipped decision (recommended), or move Shop back."
- **Problem:**        Spec↔code drift (Tier-3): the spec's §13 HUD law still claimed a top-right "wallet + Shop", but the owner relocated the Shop to the bottom chrome on 2026-06-13. The shipped code is the reality; the spec was stale.
- **Type:**           follow-up (spec reconcile to owner's 2026-06-13 Shop-relocation decision)
- **LLM-reliability:** high — deterministic doc-vs-code reconcile; the shipped layout is fully readable in source and the edit is verified by quoting it (no perceptual/aesthetic call).
- **Human-in-loop:**  none — the owner already decided (2026-06-13) to move the Shop to the bottom bar; this only makes the spec follow. No design call remains.
- **Verification:**   quoted `hud.gd:69-70` (top cluster = currencies only; Home rides its left) + the board bottom-left `[◀ Home][🛒]` (`board.gd:231-276`) and the map bottom-right Shop beside the gear (`map.gd:802-834`); §13 HUD law now matches the shipped UI; no code changed.
- **Iterations:**     1 pass; Dev caught a miss: N.
- **Result:**         done — §13 "HUD law" clause in `docs/design/merge_spec.md` rewritten to the bottom-bar reality on branch `feat/t25-spec-hud-law` (the dispatcher merges by branch). No `.gd` behavior touched; Shop NOT moved back.

### T46 — Shop storefront UX pass (buy funnel + scarcity + close + art)   ·   2026-06-16   ·   ux   ·   open
- **Asked:** Dev review of the Shop screen vs two reference cozy/merge shops, then "work on all of these in a new worktree, let me know when ready to merge." The agreed scope (the BACKLOG "Open — Shop screen" section): (1) de-grey the cards + give every card a real green BUY button (white text on green); (2) item art is the hero — enlarge + a tinted plate behind each icon; (3) clearer section separation (tabs OR bolder banners); (4) scarcity — red dots where actionable + a real "time left" chip on rotating offers; (5) IAP pouches — scale the gem art by tier + crown the best value + fix the "2× first buy" copy; (6) close button → an unmistakable red circular X; (7) the per-item "i" info badge — VISUAL only now (decorative), the live popup PARKED separately by the Dev.
- **Problem:** The storefront doesn't read as a shop. `_apply_afford` (shop.gd) desaturates the WHOLE card to `DIM_MODULATE` when unaffordable → on a fresh save (gem=0) most cards read disabled/sold-out. The price is `_price_pill` — a brown pebble (`GEM_PRICE_BG #5A3F28`, cream text) with no button affordance. Icons are tiny/faint on cream. Sections are quiet tan tabs that blur together. All six gem packs show the same cluster (no value ladder). The close uses `btn_round.png` (reads as a compass ornament). No urgency cues.
- **Type:** new (UI/presentation pass; storefront in `engine/scripts/ui/shop.gd`, dials in `tuning.gd` `class Shop`)
- **LLM-reliability:** low — perceptual/aesthetic (does the storefront read as a shop, do buttons pop, is the art legible). TRIGGER: don't ship on an eyeballed screenshot — verify with composite/zoom captures, prove the de-grey on a real fresh-save (gem=0) state, and leave the final aesthetic sign-off to the Dev's eye.
- **Human-in-loop:** recommended — final cozy-vs-clarity aesthetic call is the Dev's; sibling low-reliability UI tasks: the HUD/chrome "sticker" polish + badge-system items (in-flight on `agent-a7…`), board&quest polish (in-flight on `board-polish`). Coordination flag: the shared "sticker" red-dot badge is being built in another worktree (`skin.gd` `_RimOverlay`/`Tune.UiSkin.RIM_LIGHT`, unmerged); this task keeps its badges self-contained in `shop.gd` and parks a consolidation note rather than depend on an unmerged branch.
- **Verification:** headless suite green (`make test` → 478 passed, 0 failed; featured/layering/mechanics/save spot-checked). Visuals proven by composite captures (`make shot-map MODE=shop`), zoomed per region, NOT eyeballed: (a) DE-GREY — a forced empty-wallet capture (temp `shop_broke` mode, set_first_purchase_made + 0 currency, reverted after) shows every card stays full-brightness cream and ONLY the BUY pill takes the muted-green need-state (the headline fix, proven on the exact state that used to grey out); the affordable capture shows vivid green/white pills. (b) green BUY capsule (white on `Pal.BTN_PRIMARY`) on every card incl. the $ packs + welcome. (c) hero icon/preview on a honey plate. (d) honey section banners (Quick help / Featured / Dewdrop pouches). (e) Featured countdown chip "↻ 18h 9m" = real time to next UTC-midnight daily rotation (rotation_seed = day index). (f) red claimable dots on the reroll button + the welcome gift. (g) gem clusters scale 80→13000 by tier; "Popular" on the $9.99 anchor + "Best value" on the $99.99 whale (shown once the first-buy doubler is spent; the doubler, when active, correctly overrides both on every pack — "First buy x2", copy fixed from the confusing "2× first buy"). (h) close button = a red disc + white ✕ (was the `btn_round` art that read as a compass). (i) blue "i" info badge on the product cards (decorative — see deviation).
- **Iterations:** 1 build pass; one self-caught miss before any capture — the per-tier gem scaling would have misaligned the price buttons across a grid row, fixed by seating the scaled cluster in a fixed-height (GEM_ICON_MAX) centred slot. Dev miss: N (Dev sign-off pending).
- **Result:** done — Dev signed off + **merged to `main` (`70c6574`, `--no-ff`)** 2026-06-17; worktree + branch removed. Files `engine/scripts/ui/shop.gd` + `engine/scripts/core/tuning.gd` (`class Shop` dials) + this ledger. Self-contained in `shop.gd` (no `skin.gd` edit → no collision with the in-flight shared-badge worktree). **Deviations / parked tails:** (1) the per-card "i" is VISUAL only and input-transparent — a tap still falls through to the card's buy press; the live detail popup stays PARKED (Dev's call) — see BACKLOG. (2) "commission cleaner kit icon art" is art-lane — PARKED; the plate + enlarged size already lift the existing icons. (3) the BUY pill reuses the existing primary-green + a flat StyleBox shadow; the two-tone "sticker" rim is deferred to the shared HUD/chrome badge recipe (in-flight on another worktree) for consistency, not duplicated here. (4) all six $ packs use the green buy pill (not a distinct money colour) — uniform "buy" language; flag if money should read differently. (5) `Tune.Shop.DIM_MODULATE` + `X_TEX_MARGIN` are now unused (left as harmless consts; DIM_MODULATE noted legacy).

### T47 — Shop tails: info popup · badge consolidation · icon prompts   ·   2026-06-17   ·   ux   ·   open
- **Asked:** Dev picked up all three parked T46 tails: (1) make the decorative "i" badge open a real item-detail sheet (without triggering a buy); (2) fold the shop's local red dot / chips onto the now-merged shared "sticker" badge (`skin.gd` `Look.badge`/`attach_badge`/`rim_overlay`, `UiSkin.BADGE_*`); (3) cleaner kit-icon art. Also from the same turn: keep all `$` packs on uniform green (no money-colour split — no work), shop look approved as-is.
- **Problem:** the "i" is input-transparent (a tap buys); the shop carries its own badge/dot primitives built before the shared one landed (drift risk); the help-card kit icons (water/coin) read thin. Per `grove_art_pipeline §2` the **icon art is Dev-routed** — the Engineer authors prompts + processes/hooks-up; it can't self-generate — so (3) ships as ready-to-paste prompts + the hook-up path, not PNGs.
- **Type:** follow-up (T46 tails)
- **LLM-reliability:** med/low — (1)+(2) are deterministic code (self-verifiable: tap opens sheet / no buy; one badge source), verified by capture; (3) is Dev-channel + a perceptual share-gate call (the Dev/artist generates + signs off the look).
- **Human-in-loop:** required for (3) — the Dev must generate the icons from the authored prompts and eyeball the result (low-reliability art, like the grove §16 batch); recommended for (1)+(2) final feel.
- **Verification:** headless suite green (`make test` → 488 passed, 0 failed). (1) info popup — a temp `shop_info` capture (open shop → emit an "i" button's `pressed` → capture, reverted after) shows the parchment detail sheet (ribbon title + body paragraph + "Got it") opening from the "i", and the run printed `coins 1200->1200 diamonds 45->45` — proof that tapping "i" opens the sheet and does NOT buy. (2) badge consolidation — `make shot-map MODE=shop` composite shows the reroll button + welcome gift now carry the shared `Look.badge("dot")` (14px red, cream rim, standard overhang) and the "i" badge carries the shared `Look.rim_overlay` sticker rim; the local `_red_dot` + `Tune.Shop.DOT_*` dials are deleted. (3) icon prompts — authored, not code; delivered as `docs/design/shop_icon_prompts.md`.
- **Iterations:** 1 pass. One scope correction self-caught: the "i" can't "fold onto" the shared `Look.badge` (that's a red dot/count, not an info glyph) and the BUY/clock pills are `PanelContainer`s (a Container overrides a child rim_overlay's anchors → it'd draw inset wrong), so the sticker rim went on the "i" Button (non-Container, composes cleanly) and the red dots onto the shared badge; the pill keeps its border+shadow. Dev miss: N.
- **Result:** done — **merged to `main` (`01bb6f8`, `--no-ff`)** 2026-06-17; worktree + branch removed. Files `engine/scripts/ui/shop.gd` + `engine/scripts/core/tuning.gd` (`class Shop`) + `docs/design/shop_icon_prompts.md` + this ledger. **(1) info popup + (2) badge consolidation are shipped code.** **(3) icon art is HANDED to the Dev** — ready-to-paste prompts authored; the Dev/artist generates, then I process + hook up + verify (`grove_art_pipeline §2`); it stays parked as a Dev-channel item. **Finding surfaced:** the shop's icons are the shared currency/utility canon (acorn/dewdrop/water + watering-can), used in the HUD wallet + every price pill — so "cleaner shop icons" is really the §8 currency-icon canon; do that set as one batch (noted in the prompts doc + backlog).

### T48 — Retire the dark stat_chip pill (panel_chip) ahead of the UI-language redesign   ·   2026-06-17   ·   ux   ·   done
- **Asked:** Dev pointed at the on-board "Burst L3 ▸ 1800" pill ("this stupid semicircle pill thing") and asked where else it's used, then: "remove all of this and related UI component, keep track in the backlog for revamping and regenerating necessary assets — we will work on the UI language redesign completely." Dev calls on the two open scope questions: (Q1 blast radius) **all three** dark-chip uses — burst pill + vault gem-balance + merchant sell tag — strip the shared widget now, the displays go blank until the redesign rebuilds them. (Q2 burst sink) **keep the coin sink in code** (`burst_lvl` / `burst_count` / `_upgrade_gen_burst` / cost ladder) — only its UI goes; re-surface it in the new language.
- **Problem:** `Look.stat_chip()` (`skin.gd`) → `kit_panel("chip")` → the `panel_chip.png` dark nine-patch is the old shape-language eyesore the locked UI-redesign spec ([2026-06-17-ui-language-redesign-design.md] §6) is collapsing. It is load-bearing in exactly three live surfaces (burst-upgrade pill `board.gd`; vault gem balance `vault.gd:74`; merchant drag sell-tag `merchant_stand.gd:60` + its board-side show/hide/icon-swap wiring). The widget is being retired now so the redesign rebuilds these in the Rest-plane cream-chip language rather than restyling the dark capsule in place.
- **Type:** follow-up (UI-redesign precursor; precedes the BACKLOG "HUD, currencies & button chrome" rebuild). NOT a regression — deliberate removal.
- **LLM-reliability:** **high** — this is a deterministic removal verified by the test suite + a clean headless boot, NOT an aesthetic call. The interim "blank" state is the Dev's explicit choice, not a quality judgment; the *look* of the replacements is the redesign's (low-reliability) work, parked.
- **Human-in-loop:** none for the removal itself (tests + boot prove it). The replacement look is Dev/redesign-owned (parked in BACKLOG) — recommended there.
- **Verification:** headless suite green — `make test` → **482 passed, 0 failed** (the at-exit lambda/RID-leak lines are this project's standard teardown noise, not failures). `make smoke` → **SMOKE OK** (board + merchant + UI instantiate cleanly without the widget). Coupling proven, not assumed: (a) the burst **coin sink** is intact + tested — `_upgrade_gen_burst` buys/spends/stacks/caps/persists all PASS, and the removed pill's only unique invariant (broke refusal → no level, no coin debt) was re-expressed at the sink level and PASSES ("broke: the burst-upgrade refuses — returns false" / "broke refusal leaves no level and no coin debt"); (b) the merchant **stall-brighten** affordance survives and PASSES ("W3: dragging brightens the merchant stall"), and the merchant's kept reward pill ("top ▶ +1", a separate code-built pill, NOT stat_chip) still PASSES; (c) the vault piggy-bank card + claim PASS (the jar conveys balance; the removed number-chip was redundant). Grep confirms **zero live references** to `stat_chip`/`burst_chip`/`merchant_sell_tag`/`_try_buy_burst`/`_swap_tag_icon`/`panel_chip` — only the retirement-note comments remain.
- **Iterations:** 1 pass, no rework. One scope judgment held: kept the burst spend API (`_upgrade_gen_burst`) and the merchant stall-brighten (the surviving affordances) rather than deleting the whole feature — only the dark-pill *reads* went, matching the Dev's Q2 ("keep the sink"). Dev miss: N (removal verified by tests, not eyeball).
- **Result:** done — _(commit pending; on `main` per project rule — no branch)_. Files: `engine/scripts/ui/skin.gd` (removed `stat_chip` + the `kit_panel` "chip" arms), `engine/scripts/scenes/board.gd` (burst pill funcs + merchant sell-tag wiring + the `_show_sell_affordance` tag block), `engine/scripts/ui/vault.gd` (gem-balance chip), `engine/scripts/ui/merchant_stand.gd` (sell-tag build + return keys), `engine/scripts/ui/hud.gd` (stale `panel_chip` comment), `games/grove/tests/grove_tests.gd` (burst-pill block → sink broke-refusal; merchant sell-tag block → brighten-only), deleted `games/grove/assets/ui/kit/panel_chip.png` (+ `.import`), plus the redesign-spec "Precursor" note + the BACKLOG rebuild item. **Parked (BACKLOG "HUD, currencies & button chrome"):** rebuild the three blanked reads (burst buy · vault balance · merchant sell-value) in the new chip language + regenerate/code-build the chip asset; fold the orphaned `UiSkin` chip dials into the `tuning.gd` rework.

### T50 — UI-language redesign Phase 1: palette role tiers + sage board surface   ·   2026-06-17   ·   ux   ·   done
- **Asked:** "kick off phase 1 in a new worktree" — the palette foundation of the UI-language + colour redesign (spec + 4-phase plan committed on `main`; BACKLOG section added).
- **Problem:** `games/grove/grove_palette.gd` was a flat list of ~30 colours with no figure/ground — `GROUND #3F6B43`, `BRAMBLE_BG #4A5A3A`, `BTN_PRIMARY #4E7C46` all dark olives within ~15 points, so locked/surface/CTA were perceptually one depth. Phase 1 introduces the semantic role-tier layer (`SURFACE`/`LOCKED`/`NEAR_*`/`CARD_PEDESTAL`/`ACCENT_*` …) at the locked cool-sage values, and re-points the four board-surface consts (`GROUND`/`GROUND_EDGE`/`BRAMBLE_BG`/`BRAMBLE_EDGE`).
- **Type:** new (Phase 1 of 4). Spec: `docs/superpowers/specs/2026-06-17-ui-language-redesign-design.md` §1–§3; plan: `docs/superpowers/plans/2026-06-17-ui-redesign-phase1-palette-foundation.md`.
- **LLM-reliability:** **high** — the palette is data; the headless guard asserts the role tokens exist and the figure/ground relations hold (surface desaturated, locked recedes by value, `ACCENT_CTA`≠`SURFACE`, the old `GROUND`≈`BTN_PRIMARY` collision resolved) — deterministic, self-verifiable by `make test`.
- **Human-in-loop:** none for the data. The *visible* board shift (Phase 2) is an owner eyeball — recommended there, not here.
- **Approach / Iterations:** 1 clean TDD pass — wrote `engine/tests/palette_tests.gd` (red: 3/18), added the role-tier block + re-pointed the four consts (green: 21/0), registered the suite in `Makefile` `ENGINE_TESTS`.
- **Verification:** full headless gate green — **all 18 suites 0 failed** (palette **21/0**, grove **479/0**, layering 55/0, save 71/0, mechanics 58/0, …). Added the measured-screenshot tool `games/grove/tools/shot_sample.gd`, **but its sage assertion is DEFERRED to Phase 2**: the visible board field is the painted `bg_grove_board.png` backdrop plus a **hardcoded** cell-well `Color("#C7BB94", 0.55)` at `board.gd:1157` — neither reads `Pal.GROUND` (the consts are aliased but only `GROUND_EDGE` is used as a draw colour, for a font outline). So the palette repoint is correct as *data* but does not yet move the board pixels. **Phase 2 must** migrate the hardcoded well → `Pal.CELL_EMPTY`/`SURFACE` and reconcile the painted backdrop (asset retint or a `SURFACE` fill).
- **Result:** **VERIFIED (headless), committed on `t50-ui-redesign-palette`** (`620d533` + the sampler tool), branched from `daccb47`. Files: `games/grove/grove_palette.gd`, `engine/tests/palette_tests.gd`, `Makefile`, `games/grove/tools/shot_sample.gd`. **NOT merged** — Dev merges later (repo batch pattern; global "commit/push only when asked"). Coordination: pairs with `t49-chapter-rename` (Phase 3 — chapter ribbon already deleted there); Phase 2 picks up the board cell migration noted above.

### T51 — UI-language redesign Phases 2–4: board · HUD · order strip · nav · overlays   ·   2026-06-17   ·   ux   ·   done
- **Asked:** "kick off phase 1 in a new worktree" → "continue / go on phase 3 / just finish all redesign need for the UI and merge." Carry the locked palette + spec through the board, HUD, order strip, navigation, and overlay-inheritance phases, then merge.
- **Problem:** Phase 1 landed the palette as *data* but the board still read dark — a hardcoded olive cell-well + a painted backdrop + dark bramble/fence/nav chrome. Phases 2–4 apply the three-plane depth ladder + light-warm palette across the board and chrome so locks recede, items pop, and green is reserved for the CTA.
- **Type:** new (Phases 2–4 of the redesign). Spec: `docs/superpowers/specs/2026-06-17-ui-language-redesign-design.md`. Owner mid-flight revised the surface **cool sage → light warm-neutral `#EDE6D2`** (the `llm_board3` render) and **de-greened the level token → honey `#EAD49C`**; both applied.
- **LLM-reliability:** **high for structure** — every step is a static, testable builder (`_cell_style`/`_field_backdrop`/`_locked_style`/`_quest_band_style`) guarded headlessly + a foundation guard for the palette/elevation/de-green relationships; **med for look** (final feel is an owner eyeball, captured via `make shot-grove`).
- **Human-in-loop:** recommended on the look (screenshots shared each milestone; owner picked the palette + token colours and the flat backdrop).
- **Approach / Iterations:** incremental TDD, one small tested commit per sub-step, each preceded by a parallel-reader *understand* pass and followed by an adversarial *verify* pass (the ultracode loop). **Phase 2:** Sunk elevation tier (`tuning.gd`) · empty cell → `CELL_EMPTY` · flat `SURFACE` field backdrop · locked cells recede on the Sunk plane (`piece_view.gd` — light `LOCKED` well + quiet `LOCKED_GLYPH`, dropped the dark `bramble_*` overlay + cream-on-bark chip) · removed dead bramble shader/art. **Phase 3:** de-green the level token (honey + ink number) · lighten the quest band to a Rest strip + enlarge the giver bust 124→150 · lighten the board bottom nav (cream Rest panel + home icon) · removed dead `fence_grove.png`. **Phase 4:** overlay audit (shop/overlays found coherent) — softened the shop countdown chip, aligned `ICON_PLATE_BG` to `CARD_PEDESTAL`, removed dead `DIM_MODULATE`. Also processed the **12-icon chrome kit** from `assets/llm/llm_icons.png` (new `games/tools/slice_grid.gd` band-detector + `process_icon`) → `ui/kit/icon_*` (9 restyled + new `bag`/`map`/`sprout`).
- **Verification:** **full headless gate green at every step** — final: palette **25/0**, grove **489/0**, all engine suites 0-failed, `make smoke` OK. Three workflow *verify* passes: step-4 locked cells (mechanics + visuals clean; caught dead code + thin tests, both fixed), Phase-4 overlay audit (coherent), and a final cumulative `main...t51` verify (regression **clean**; fixed one green-as-structure met-badge + three orphaned aliases). New/extended guards: foundation (`palette_tests` — role tiers, Sunk≤Rest≤Raised, de-greened token) + grove static-builder asserts (no-TextureRect / no-PanelContainer on a built locked cell, quest band light).
- **Result:** **VERIFIED, merged to `main`.** Branch `t51-ui-redesign-board` (off `daccb47`, with `main` merged in for the light palette + icon kit). The board reads light top-to-bottom; the only deliberate dark remaining is modal scrims. **Deferred (parked, not regressions):** the §2 textured garden-bed backdrop (flat `SURFACE` ships; a soil-tile pass remains), the §4 item contact-shadow grounding + `ITEM_BOX` optical scale, the map-page painted-context dark chrome, and pre-existing `#33402F`→`Pal.INK` / gem-FX colour-token consolidations. `bg_grove_board.png` is now unreferenced (kept for the textured-backdrop follow-up).

### T53 — Mystery-reward dialog: externalize login rewards to JSON + workbench reveal preview + spin pacing   ·   2026-06-23   ·   ux   ·   done
- **Asked:** "Mystery-reward dialog — build + verify the full experience on placeholder rewards." Three parts: (1) externalize the daily+mystery reward config (`LOGIN_LADDER`/`LOGIN_MILESTONES`/`LOGIN_MYSTERY`) from GDScript consts to a JSON `login.gd` reads; (2) live end-to-end check of the animated spin-reveal, tune `_spin` step counts/delays if the deceleration reads off; (3) throw away `.scratch/mystery_amounts_shot.gd` and build the mystery-reveal capture/preview into the UI workbench.
- **Problem:** the reward tables were code constants (not data-tunable without a code edit); the spin pacing had only ever been verified mid-spin (a still) + by the instant-grant test, never watched/measured end-to-end; and the only visual aid was a throwaway scratch script outside the repeatable workbench.
- **Type:** follow-up (of T44 login calendar + the §18·"T46" mystery-days work; was backlog follow-ups 1.3/1.4 + the Active item).
- **LLM-reliability:** **mixed.** Parts 1 + 3 are **high** — data plumbing + a workbench builder, self-verifiable by `save_tests`/`login_tests` (login math over the JSON) and `grove_workbench_tests` (the preview renders). Part 2 (the deceleration *feel*) is **low** — perceptual; mitigate by COMPUTING the spin durations (deterministic) + rendering the key states in the workbench, and surface the final feel call to the Dev rather than eyeballing.
- **Human-in-loop:** recommended on the spin *feel* (part 2) — same low-reliability kind as the other animation/pacing tasks; the measured durations + the workbench states are shared for the Dev's eye. None for parts 1 + 3 (deterministic).
- **Approach:** **(1) JSON** — added a lazy-load+cache JSON reader to `core/login.gd` mirroring `core/strings.gd` (`_config()` off `Game.active()` → `games/<active>/login_rewards.json`, `_reset()` test hook, graceful-missing); re-pointed every accessor (`reward_for`/`is_milestone`/`is_mystery`/`mystery_pool`/`roll_mystery`/`water_safe_max`) at the config, converting the milestone/mystery-slot lookups to **string keys** (JSON objects key by string). Deleted the four `LOGIN_*` consts from `grove_data.gd` (grep-confirmed only `login.gd` read them) → JSON is the single source, no mirror/drift; left a pointer comment preserving the faucet-discipline rationale. **(3) Workbench** — extracted `LoginMystery.build_reveal(options, winners, width, opts)` (+ `reveal_width(vw)`) as the SINGLE source for the reveal face; `open()` now calls it then wraps the spin/grant, so live + preview render byte-identically. Added a `mystery` workbench component (deterministic demo roll from the new `Login.mystery_config(slot)` — first `show` pool entries, evenly-spaced winners, no shuffle; states "shown"/"won"), and a general **focus-capture** mode (`_focus_only` → `make shot-workbench EL=<id>`) replacing the deleted scratch. Routed the "You won!" caption through `Strings.t("mystery.won")`. **(2) Pacing** — computed the spin durations + sampled a live 15-frame run; the curve reads right so `_spin` left untouched, day-7 length flagged to the owner (BACKLOG Tuning).
- **Verification:** **`make test` 812/0** (20 suites) — incl. `save_tests` **77/0** (the 21 mystery + ladder/milestone/faucet assertions now read the JSON), `login_tests` **15/0** (run direct; still `_DISABLED`), `strings_tests` **4/0** (validates `mystery.won` resolves + no leftover literal `tr`), `grove_workbench_tests` **207/0** (+13 new: mystery registered + gallery-neighbour, `build_reveal` shape, day-7 amounts/determinism/shown-vs-won dimming, day-4 pool, `reveal_width` 560 cap, preview-only). The disabled `grove_economy/ui/placement` suites fail **17** both WITH my changes and at a stashed `d5ab1d3` baseline → **zero new breakage** (pre-existing; unrelated to login). Pacing **measured** (day-4 spin 1.58s, day-7 3.60s, +1.5s hold) and **captured** live (sweep→land→"You won!"→celebrate→auto-dismiss frame strip, shared with the Dev) — not eyeballed. Static reveal verified via `make shot-workbench EL=mystery` (faithful: banner/cards/amounts/winners-lit).
- **Iterations:** 1 pass, no Dev miss yet (handoff pending owner feel sign-off on day-7 pacing). Caught + fixed in self-review: the `_set_highlight` scale-pop pivots wrong on an unlaid-out preview → the "won" state uses modulate-only; the reveal width must use the engine `min(560, 94%)` rule (not the workbench `width_pct`) so the preview matches the phone dialog.
- **Result:** **VERIFIED, merged to `main`** (`--no-ff`, worktree `worktree-mystery-reward-dialog` removed). Files: `games/grove/login_rewards.json` (new), `engine/scripts/core/login.gd`, `engine/scripts/ui/login_mystery.gd`, `games/grove/grove_data.gd`, `games/grove/strings.json`, `games/grove/tools/ui_workbench{,_view}.gd`, `Makefile`, `games/grove/tests/grove_workbench_tests.gd`; deleted `.scratch/mystery_amounts_shot.gd`. **Parked (Dev's, in BACKLOG):** mystery-pool *number* tuning (Tuning); the day-7 spin-length feel call (Tuning, with the exact knob); re-enable `login_tests` in the Makefile (Testing).

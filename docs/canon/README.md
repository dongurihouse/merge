# docs/canon — the VISUAL ground truth (eng rule 15)

Owner-accepted reference images + quiet captures of owner-accepted states.
LOOK-class boxes (layout style, palette, art direction) name a target HERE and
the DONE entry shows a SIDE-BY-SIDE against it. "Matches the canon" is the
claim; a lone crop is not acceptance.

## Index
- `ref_merge_calm.*` — OWNER REFERENCE (2026-06-12): the comparable merge game
  the owner cited for the fence redesign (AB) and the calm pass (AF). Read it
  correctly: its play surface is LIGHT and LOW-CONTRAST relative to the pieces,
  but it is **warm, textured (soft wood), and full of soft depth/shadow** — it
  is NOT pale, NOT desaturated, NOT flat-fill. (Owner: drop the screenshot file
  in here; this line reserves its slot.)
- `board_calm_target.*` — the agreed end-state of the board after AF (capture
  once AF's owner eyeball passes).

## How to add
Owner drops a reference screenshot in here and names it in the order. Eng/
Director captures accepted in-engine states via the quiet tools (never a
visible window).
